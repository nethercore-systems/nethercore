[package]
name = "lighting"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = false  # Keep debug symbols for better error messages
debug = 1  # Include line number info for backtraces

[workspace]
