[package]
name = "font-demo"
version = "0.1.0"
edition = "2021"
description = "Demonstrates loading texture atlases for bitmap fonts"

[lib]
crate-type = ["cdylib"]

[dependencies]
libm = "0.2"

[profile.release]
opt-level = "s"
lto = true

[workspace]
