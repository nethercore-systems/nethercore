[package]
name = "tracker-demo"
version = "0.1.0"
edition = "2021"
description = "XM tracker music playback demonstration"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

[dependencies]
libm = "0.2"
