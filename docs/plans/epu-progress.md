# EPU implementation progress

**Standing creative direction — retain regular RADIAL and the restored organic capability.** The shown regular result “looks fine” to Robert; WARPED_RADIAL now coexists and has the bounded technical pass recorded below. Its substantially coarser appearance still needs Robert’s verdict. Neither scoped result is whole-EPU visual acceptance.

## Current checkpoint — working product paused for the usage break

**CI-only follow-up:** the first hosted run passed format/bindings but Rust 1.98
Clippy rejected 45 fixed-size slice-chunk calls in the GPU harness before tests.
The authorized repair changes only those iterator calls (19 test files), not
runtime/SDK/shader code, fixtures or tolerances. Local workspace Clippy and 8
representative GPU tests pass; a standard-library equivalence probe covers all
22 chunk sizes / 174 boundary lengths. The per-commit hosted run is still the
final CI gate. The standing goal is explicitly paused; after this CI follow-up,
resume only on Robert's instruction. See `epu-checkpoint.md` for the bounded scope.

**Resume guide: [epu-checkpoint.md](epu-checkpoint.md).** The user explicitly
authorized a safe commit pushed to `main`, superseding the earlier no-commit /
no-push restriction. Work is paused until Robert resumes it; historical next-step
rows below are not instructions to keep running unattended.

Full workspace: **1,766 passed / 0 failed / 50 ignored**, including **167 GPU** and
**539 ZX library** passes. The final packaging fix has a focused RED → GREEN and
**82 CLI tests passing**: host tests/examples/build scripts no longer masquerade
as game WASM targets. Both showcase and inspector build and pack normally.
Four final-player native replays pass; city/Grove/Arcade are pixel-identical to
their retained references, and all four owned players were reaped. The only final
shader cleanup removes one blank EOF line; its three SCATTER_PHASED GPU checks
pass without changing executable text. Clippy/FFI and the local book/snippet
checks pass: **7 changed pages / 389 links / 187 anchors / 256 phase values**.
Test fixtures, their byte-preserving Git rules, current limitations and exact
restart commands are durable in source. Hosted CI is a separate commit-linked gate.

**Full goal remains open.** TRACE/POLAR's opposite-pole defect is documented, not
hidden with an unauthorized fade. Hermes owns the remaining bounded engineering;
Robert owns visual/usefulness acceptance; performance waits for an explicitly
quiet GPU window. No visual or performance verdict is implied by this checkpoint.

## Prior checkpoint — non-fading PATCHES pole repair technically closed

**Goal not complete.** `tmp/epu-review/patches-pole-closure/closure.json` seals the coordinate-only CYL/POLAR repair using the unchanged existing azimuth helper. Focused RED **120 pole failures → zero**, 240 injected discontinuities rejected, and **49,152 exact outside-cap comparisons**. No opacity/support fade was added; region patterns and their weighted alpha can change inside the coordinate cap. The registered PATCHES checks (5), shared-helper checks (5), and VEIL preservation checks (3) pass. These are focused current checks, not a new broad-suite total.

**Real unchanged cartridges:** 14 cases / **7,257,600 pixels**, zero failed or excluded pixels, maximum error **1** at fixed limit **2**. Both direct/reserved controls remain pixel-identical. Fourteen full-footprint cache checks cover 14,336 cells, maximum error 0.00048828125 at .002, with wrong inputs rejected. All **28** capture players were reaped. Current SDK/helper preservation, published driver and five-page local docs pass: **298 links / 155 anchors**. The finite audit/closure and original failure receipts remain retained.

**Next ownership boundary:** all 27 retained showcase programs were checked for PATCHES CYL/POLAR callers; none use the changed charts, so no speculative gallery re-render is justified. The read-only `remaining-poles` audit separately records BAND's bounded pole pass and TRACE's 15 opposite-pole failures. TRACE has not been edited. Mirroring its existing first-pole fade would change visibility and needs a separate decision; VEIL-only authorization is not generalized. Hermes retains broader family/documentation engineering ownership; Robert owns the separate finite environment usefulness/appearance verdict. Performance acceptance remains deferred.

## Prior checkpoint — authorized VEIL polar repair technically closed

**Goal not complete.** `tmp/epu-review/veil-polar-boundary/closure.json` verifies the explicitly authorized mirror of the existing AXIS_POLAR support envelope at the opposite pole. Only that coverage behavior changed; existing widths are retained. GPU RED **8 pole-limit / 192 envelope failures → zero**, with **33,792 exact outside-cap comparisons** and nonzero interior coverage. The unchanged regression fixture passes, along with eight existing VEIL GPU checks and 39 editor tests. This is focused post-repair evidence; the 165 GPU / 539 library totals below belong to the earlier authoring checkpoint, not a new broad run.

The unchanged Rust-export / canonical-SDK / WASM / packed-cartridge/native route passes **10 cases / 5,184,000 pixels**, zero failed or excluded pixels, max **1** at fixed **2**. All ten reversed-direction controls reject. The front-pole, DIRECT3D, approved cylindrical-rain input and TANGENT_LOCAL control captures remain pixel-identical. **10 cache cases / 10,240 cells** pass at max **0.000291893306957669** against **.002**, with ten rejecting wrong-direction controls and no omitted folds/corners. Before/after guest bytes are unchanged; all **20 native capture players** were reaped, and the exact checkout/target idle guard passed.

Local documentation and published snippets pass again: **5 pages, 296 links, 153 anchors, 256 published phase steps**. SDK/C/Zig edits are confined to VEIL documentation; outside-section API/helper bytes remain exact. The retained-frame verifier recomputes pixel/reference comparisons without another GPU run and checks the current source/player identities. Its initial LF-only byte-pattern and whole-manifest cache comparison assumptions were corrected to the actual CRLF bytes and cache renderer-only scope; no tolerance, image, shader or historical receipt was changed by those verifier repairs. Exact original-resolution before/after card: `tmp/epu-review/veil-polar-boundary/veil-polar-support-repair.png`. Technical support-envelope acceptance is not a human visual verdict.

**Hermes resumes:** reconcile the remaining named family coverage and documentation acceptance, reusing unchanged evidence. **Robert owns:** the separate finite environment usefulness/appearance review. No new authorization is required for the sealed polar repair. Broad EPU technical acceptance and later performance acceptance remain open.

## Prior checkpoint — VEIL authoring closed; polar coverage decision subsequently authorized

**Goal not complete.** `tmp/epu-review/veil-authoring/closure.json` seals the bounded inspector/API correction: four authoring RED failures became GREEN; 39 editor tests, 165 GPU tests and 539 library tests pass (two GPU timing probes and one library timing probe ignored). VEIL's integer count, count-dependent width readout, variant-specific phase/shape activity and PILLARS Color B/glow inactivity now match the evaluator. All 256 raw count bytes survive idle, all 31 count edits encode exactly, and 128 GPU control/domain cases (84 active, 44 inactive) pass over 393,216 readbacks. Renderer executable WGSL, packed fields, FFI declarations/helpers and every showcase recipe remain unchanged. The rebuilt real player preserves City, approved Grove and approved Arcade across 1,555,200 pixels with zero changed/failed/excluded pixels. Owned players reaped.

Local API/guide/book/Rustdoc and the real WASM inspector build pass; five local pages, 296 links, 153 anchors and the exact published 256-phase driver are checked. `cargo xtask ffi generate/check --console zx` passes with documentation-only binding changes. The initial single-expression GPU equality diagnostic had a tiny residual; its retained failed receipt is not production RED. The qualified probe uses one interleaved evaluator call and retains exact zero tolerance for inactive output. The dense-count pointer fixture now uses painted rail/handle geometry (including rectangular handles), not estimated label spacing; no production snapping workaround remains.

**Historical polar RED (superseded by the verified authorized repair above):** `tmp/epu-review/veil-polar-boundary/before.json` retained eight path-dependent opposite-pole rows / 1,360 actual shared-GPU samples at fixed .005. Robert selected “Authorize mirroring the existing support fade at both poles”; `authorization.json` binds that decision to the frozen native contract. The later closure retains this RED and the prior first-pole behavior; no geometric non-fading replacement was introduced.

**Finite composition review prepared, not accepted:** `tmp/epu-review/finite-breadth-review/review-set.json` selects eight existing source-bound frames covering the three intended space types and seven requested breadth categories. Two exact-pixel cards are prepared; no new recipes or algorithms were added. City/Desert/Grove/Graveyard and Station/Arcade/Cavern/Sky Ruins are concrete review questions, not proof of named scene identity. Delivery inspection finds readable complete panels, but several themes remain visually ambiguous; do not promote intention labels to usefulness. Existing Grove/Arcade repair approvals remain preserved, not reopened. Human usefulness/appearance and later performance acceptance stay separate.

Family inventory reconciliation: all 24 current metadata opcodes have individual records below; inventory/global regression does not promote partial records to VERIFIED. FLOW's historical `audit.py aligned` exit 1 was already reconciled by `flow-streak-lanes/aligned-reconciliation.json` as the original 646-failure behavioral RED, not an unresolved adapter error. The requested legacy SCATTER export/runtime refresh remains closed in its recorded scope. Historical failures and earlier source-bound receipts are retained, not silently rebased to the new player.

## Prior checkpoint — city structure technically repaired; broader goal and human gate open

- **Neon Metropolis, guest recipe only:** the unchanged default view originally classified all **32,400** probe rays as floor. A two-field SILHOUETTE repair (height bias raw **188 → 0**, wall depth raw **0 → 128**) gives **2,109 sky**, **19,020 wall** and **4,012 floor** core samples, **139 skyline columns**, and a **62-row** roofline range. The frozen old values still fail. Existing Grove criteria/camera were reused; **7 structural tests pass**.
- Actual current Rust inventory → real WASM → packed showcase verification proves **exactly two bytes** changed in `s0-0`, at byte offsets **43 and 46**; all **26 other scene programs / 215 other slots** remain exact. A detected native-player relink was retained and reconciled by replaying the old city cartridge plus the candidate and approved Grove/Arcade on the final player; all four frames match their capture references exactly. No shaders, camera, palette or other layers changed.
- Incidental rustfmt recursion into the adjacent Shrine recipe was restored byte-for-byte. A fresh canonical-source WASM build is **byte-identical** to the already tested guest. Evidence: `tmp/epu-review/city-structure/{closure.json,payload-proof.json,post-tests.json,canonical-source.json,review.json}`. The updated city still requires **human usefulness/appearance review**; geometry visibility is not a city-art acceptance verdict.
- **SPLIT/SURFACE packed-route gap closed without production changes:** **21 cases / 10,886,400 native pixels**, no failures/exclusions, max **1 / limit 2**; all eight/four variants are visible, inactive-domain pairs are exact, and **21 cache checks** pass. See `tmp/epu-review/split-surface-packed/closure.json`.
- Earlier carrier-loop, pole and gallery evidence stays sealed to its stated source/player cohorts. Their renderer sources are unchanged by this recipe repair; do not relabel the historical full regression as a fresh run. Remaining finite matrix/documentation reconciliation and useful composition coverage are Hermes-owned. **No human or performance acceptance is claimed.** All owned test players were reaped.

## Prior checkpoint — carrier loop technically closed; composition acceptance open

- **Carrier-loop RED → GREEN:** ADVECT/MASS shaping mixed cyclic time with fractional static-shape frequencies. Separated cyclic time while preserving the original static relief helper. **1,008 wrap failures → 0** across 5,304 qualified rows; 1,357,824 packed/continuous bridge records and 8,192 exact phase-zero shape checks pass. MOTTLE's four variants remain preserved controls; its unused domain bits are invariant, not additional algorithms.
- **Real guest closure:** 36 exact Rust export → current SDK → WASM → packed-cartridge cases (18,662,400 pixels), plus 9 coupled wrap captures (4,665,600 pixels). Zero failures/exclusions, maximum error 1, fixed limit 2; 36 matched-footprint cache checks pass and wrong-byte controls reject. Exact 264-frame rollback and 512-phase published driver pass. See `tmp/epu-review/carrier-temporal-loop/closure.json`.
- **Current source cohort:** 163 GPU + 537 library tests pass; timing probes remain ignored. Local docs: 5 pages, 294 links, 151 anchors. FFI changes are documentation-only. Earlier pole/PLANE receipts retain their historical source identities; the current suite includes their regressions.
- **Preservation and composition:** Grove and Arcade are pixel-identical to their approved references. The unchanged real showcase cartridge has **27 unique current-source gallery captures**, including all 11 affected carrier callers; guest-state hashes match the packed exports. `tmp/epu-review/carrier-temporal-loop/gallery/receipt.json` binds every image, replay, cartridge and player. This is current runtime/material evidence, not independent whole-gallery reference parity or proof of compositional usefulness.
- All owned players are reaped. No public API/cartridge layout changes, commits, pushes, hosting, copied build trees, or performance acceptance. **Human visual/usefulness review and finite ledger reconciliation remain OPEN; the whole plan is not complete.**
- Retained verifier-draft failures were file/schema assumptions and a CRLF-normalized scratch archive, not renderer failures. The archive was recovered only after matching focused-GREEN, full-suite and native-capture digests. See `verifier-schema-reconciliation.json`, `candidate-common-eol-recovery.json`, and `published-driver-eol-reconciliation.json` beside the closure. Inputs, tolerances and pixel scope were unchanged.

## Prior checkpoint — shared transport poles technically closed; broader goal open

- `tmp/epu-review/transport-chart-poles/closure.json`: actual ADVECT/MASS CYL/POLAR pole brackets expose **94 → 0** failures across **11,520** samples; final maximum **0.00024414063 / .005**. Same decoded axes and samples, exact pole values, bilateral approaches, same-side controls, and rejecting injected jumps are retained. Shared-noise GPU suite: **3 passed**; actual editor suite: **37 passed**. This is focused current evidence, not a new full-suite claim.
- The shared azimuth helper closes the undefined angular direction only within transverse radius **0.05**. **46,080** cap-join records pass; **15,360** off-cap comparisons are exact. No opacity gate is added. The original ADVECT body, MASS source, and DIRECT3D coordinate path are unchanged; no public layout or guest-byte change.
- **24 exact exported Rust / canonical-SDK / WASM / packed cartridges** pass current native execution and matched references: **12,441,600 pixels**, zero failures/exclusions, maximum **1 / 2**. Four DIRECT3D controls and approved Grove/Arcade frames remain exact. All **24** current-SDK recompiles reproduce the original WASM bytes. **24** cache/direct-footprint checks pass (max **0.00029974858443193586 / .002**), including folds/corners and rejecting wrong-input controls. **50** owned players were reaped.
- API/guide/SDK document local chart closure; canonical C/Zig generation/check preserves all bytes outside that documentation span. Local book, Rustdoc, inspector WASM and exact 256-phase published driver pass: **5 pages, 292 links, 149 anchors**. The initial verifier omitted the newly added cap test from its owned-source allowlist; its failure is retained, and recovery verifies that removing exactly that test restores the original RED-source hash.
- `showcase-caller-inventory.json` inspects all **27** retained 32-update showcase programs: none uses the changed ADVECT/MASS CYL/POLAR domains. Do not invent an affected-scene rerender task; only the two approved controls were freshly replayed. This inventory is not human acceptance or universal runtime coverage.
- Human appearance remains Robert's gate. Hermes continues finite mechanism/control reconciliation and useful indoor/outdoor/hybrid composition coverage; performance stays deferred. No commits, pushes, public hosting, copied build trees or unrelated-service changes.

## Prior checkpoint — PLANE repair technically closed; broader plan remains open

`tmp/epu-review/plane-pattern-controls/closure.json` seals the HEX nearest-site/coverage and GRATING gap-direction repairs: geometry RED **9,344 → 0 / 12,288** at `.002`; gap-direction RED **11 → 0** over **122,880** samples at the unchanged `.005` monotonic budget. The new focused tests pass; the current broad cohort is **159 GPU + 536 library passes**, with timing probes ignored. The original foreground timeout is retained as an unsealed interruption, not a test failure.

The same **14 Rust export → canonical SDK → WASM → packed-cartridge → native-player cases** pass **7,257,600 pixels**, no failures/exclusions, **max1/limit2**. All six unaffected PLANE variants and the exact human-approved Grove/Arcade frames stay pixel-identical. Fourteen cache/direct footprint cases pass at `.002` (max `0.0004286164683844351`), with rejecting wrong inputs and no omitted folds/corners. All **30** owned native players were reaped. Only the two named shader function bodies changed; packed fields, declarations and unrelated binding/helper bytes are preserved.

API/guide/SDK/inspector guidance is synchronized; the quick-reference inventory now includes all **24 active opcodes plus NOP**. Local book/Rustdoc/inspector builds and the exact published helper pass: **5 pages, 288 links, 145 anchors, 256 driver phases**. Native comparison cards `hex-pattern-repair.png` and `grating-gap-repair.png` preserve every panel pixel; visual inspection checked delivery integrity, not taste.

FLOW's four affected full-showcase replays are complete without recipe edits. `flow-streak-lanes/aligned-reconciliation.json` proves the old aligned exit1 was the **expected 646-failure RED**, superseded by the registered zero-failure GREEN—not an unresolved audit error. Its two full-scene comparison cards are still human-pending.

**Remaining owner work (Hermes):** reconcile the remaining finite algorithm/control and useful-composition criteria, starting with loop-phase authoring metadata still advertising `/255`-style endpoints for shaders that use `/256`. **Human gate (Robert):** judge the new exact comparisons and still-unreviewed compositions; existing scoped approvals are retained. The overall goal is not technically complete, and performance acceptance remains explicitly deferred.

- **Cyclic phase metadata is technically closed:** DECAL, VEIL, MOTTLE, ADVECT, SURFACE and MASS now display raw/256 turns. RED reproduced **1,530 wrong displayed values and 18 missing readouts**; GREEN has zero, with **1,536 byte mappings and 37 editor tests**. Executable WGSL and bindings are unchanged; all stored phase values remain available. The native player was rebuilt and both approved scene frames are again pixel-identical. Owned players were reaped. Local docs: **5 pages, 290 links, 147 anchors**. Evidence: `tmp/epu-review/cyclic-phase-metadata/closure.json`. This closes phase units only, not variant activity or the broader algorithm matrix.
- Earlier PLANE render/GPU/native receipts remain sealed to their capture cohort. The subsequent six shader-file edits are metadata/comments only, with executable-source identity recorded in `cyclic-phase-metadata/comment-only-sources.json`; they are not six new rendering changes. The next owner check is actual transport-chart boundary behavior, not a repeat of the unchanged full suite.

### Retained prior checkpoint


User approved the two delivered Grove/Arcade comparisons with **“Sure looks good please continue”**. The exact artifacts and SHA-256 identities are recorded in `tmp/epu-review/arcade-floor-layering/human-review.json`; the capture-time records remain historical. This approval covers those shown repairs only, not unseen variants or the whole EPU. The authorized source/process follow-up passed with `SOURCE_DRIFT []` and `LIVE_TARGET_PLAYERS []`; the on-disk player matched the native receipt (`1016a153a4711f37a2139165110b020463db205402358f6540638c26cd34ac42`). Preserve the approved recipes rather than reopening them on a model taste judgment.

Remaining structural coverage is being closed without a shader or composition edit. `silhouette-paths/all-profile-audit.json`: **6 focused GPU tests passed**, including all eight SILHOUETTE variants and three SECTOR variants, **12,705 packed rows / 203,280 samples**, maximum wrap **0.0009765625** under unchanged **.001**; **690 profile joins / 6,210 samples**, all eight profiles nonflat, maximum final error **0.00024414063**, all **690** injected jumps rejected. A prior texture-size validation failure is retained as a harness failure, not a shader RED. The probe was tiled without dropping samples or changing thresholds. A subsequent same-side assertion plus broad non-performance GPU/library replay is running; **15** exact guest/native cases are prepared for after it. Hermes owns those technical checks and the remaining ledger reconciliation. No performance work or universal acceptance claim.


- **Authoring closure:** `tmp/epu-review/bounds-alpha-authoring/closure.json` records `BOUNDS_ALPHA_PLANE_PHASE_AND_LOCAL_DOCS_PASS`. **35 editor tests passed**, including preserved byte storage and actual pointer-input checks for inactive/common controls and active sibling controls. The earlier common-control receipt remains separately retained; unused bounds Alpha B controls were subsequently corrected too.
- **Phase and generated metadata:** PLANE/WATER now displays its actual **raw / 256 turns** without a duplicated endpoint. The metadata generator emits round-trip float literals instead of rounding to six decimal places. `metadata-precision.json` verifies **120 fields / 240 numeric limits** against canonical WGSL, with four legacy endpoint mismatches rejected and zero current mismatches. Executable shader logic is unchanged.
- **Published/SDK paths:** current local book, rustdoc, the exact **256-phase** published driver, inspector WASM, native-player build and FFI synchronization passed. Four local pages passed **233 links / 118 anchors**. Generated C/Zig bytes outside the intended PLANE documentation span are identical; no public site was changed.
- **Composition defect isolated:** all **20** floor-PLANE recipes pointed away from the floor (19 up, one sun-directed). The compiled-input direction check recorded **20 failures**. Only those direction arguments were changed to `DIR_DOWN`; cameras, materials, shader algorithms and other packed fields were not edited. The independent forest issue is an offscreen roofline/shallow wall band, not yet an accepted repair.
- **Floor native route closed:** `tmp/epu-review/composition-floor-planes/closure.json` records 21 exact baseline replays and 21 after-captures, 10,886,400 compared pixels, only 20 floor direction fields changed and 196 other slots identical. The relink was bridged by pixel-identical baselines (maximum error 0), not waived. Nineteen floor cases changed; Neon Arcade's later opaque CELL paint explains its zero delta and is a separately recorded layering repair. All players exited. `floor-direction-s0-0.png` retains both native panels at 1:1 scale for Telegram; no visual acceptance is implied.
- **Grove/Arcade and mask follow-up:** the Grove GPU RED found all 32,400 view rays owned by floor and no sky/tree wall. Only three silhouette bytes changed: current GPU output has 222 sky/wall columns and 41 rows of roofline variation; legacy parameters still reject, and four structural checks pass. Arcade's actual shader RED finds zero floor contribution because later bounds paint overwrites it. The bounds-mask pointer RED also reproduced an editable ignored control. Those two fixes are applied, with GREEN/docs/native verification pending. These are not included in the historical 35-test authoring closure.
- **Ownership and remaining gates:** unrelated separate-target SpecCade jobs are left alone; each owned replay player is waited and reaped. Broader finite algorithm/path reconciliation, useful indoor/outdoor/hybrid composition review and final human media acceptance remain open. Performance is deferred. No commits, pushes, hosting, copied build trees or unrelated service changes.


## Prior checkpoint — PORTAL technically closed; remaining EPU evidence/showcase work open

- **PORTAL SCOPED_TECHNICAL_PASS / HUMAN_PENDING:** `portal-paint-vortex/closure.json` seals source-bound RED→GREEN, authoring, **149 GPU + 527 library tests**, exact Rust export/current SDK/real WASM/packed cartridge/native-player routes, cache, docs and rollback. Paint: 19,110 failures → zero / 110,592 records with exact existing coverage weights. VORTEX: 9,030 → zero / 27,648 records, 7,884 meaningful moving samples; the restored-circle control rejects. **17 unchanged cartridges / 8,812,800 native pixels**, zero failures/exclusions, max1/limit2. The same actual guest before/after wrap trace adds **9 frames / 4,665,600 pixels**, max1/limit2 and **264 rollback frames**. All 17 cache cases pass, max .00048828125/.002; current SDK static and counter WASMs are byte-identical. Native UI phase/roughness activity preserves stored fields; book, links, published driver, Rustdoc, inspector hints and unchanged C/Zig declarations pass. Owned players stopped. The failed control-name adapter was restored against the frozen RECT bytes; no passed broad suite was rerun or gate loosened. The exact encoded 9-frame comparison is fully decoded and frames 0/4/8 pass delivery-integrity inspection only. This is slowed inspection, not a full-cycle visual acceptance. No new opcode or engine clock; human appearance, remaining EPU/showcase work and deferred performance remain open.


- **DECAL facing SCOPED_TECHNICAL_PASS / HUMAN_PENDING:** `decal-facing/closure.json` seals the shared single-sided guard. Actual input-fed RED 594 / 20,736 → GREEN zero at .002; restored-old duplication rejects and front RGB/coverage remain exact. Both facing and prior paint checks pass (2 focused tests). Eight identical exported Rust/current-SDK/WASM/cartridge before/after cases cover 4,147,200 pixels, zero failures/exclusions, max1/limit2. Only backward RECT/LINE frames change; front DISK/RING/RECT/LINE, backward DISK and LOBE control are pixel-identical. Cache, current-SDK rebuild, executable book snippets/local docs and unchanged public declarations pass; owned players reaped. No rescaling, segment length or grazing fade was added. Selected panels are exact and delivery-checked, not human-accepted. Broad regression after this guard remains pending at the next integration gate.

- **LOBE centre TECHNICAL_PASS / HUMAN_PENDING:** Robert authorized ordinary centre-bright gain. `lobe-centre/closure.json` seals the removed centre suppression: immutable GPU RED 3,150 → zero failures across 51,840 records at .002; the old-cap control rejects and palette/waveform functions remain held. Actual inspector waveform/phase controls preserve raw bytes, unused B alpha and inactive phase; a real phase edit preserves sibling bits. **146 GPU + 526 library tests pass**, with 2 performance / 1 existing timing probe ignored. Nine unchanged export/current-SDK/WASM/cartridge before/after cases cover **4,665,600 pixels, zero failures/exclusions, max 1 / limit 2**, plus nine cache cases (max 0.00048828125 / .002). Off, strobe-off and DECAL control images remain identical. Current-SDK byte equivalence, book/links/snippet, Rustdoc, example hints and unchanged C/Zig declarations pass; owned players are reaped. BAND retains explicit rings. Appearance remains Robert's gate and performance stays deferred; this is not whole-EPU completion. The initial invalid fixture/baseline was retained separately; FFI freshness was regenerated before retrying authoring.

- **RINGED tilt TECHNICAL_PASS / HUMAN_PENDING:** `celestial-ring-tilt/closure.json` was reverified. The fixed .002 GPU gate covers 65,536 records with zero failures and rejects the old kernel. **143 GPU + 525 library tests passed**; The ring native gate has **nine** unchanged export/SDK/WASM/cartridge cases, **4,665,600 pixels, zero failures/exclusions, max 1 / limit 2**, and nine cache cases (max 0.0004130528 / .002). Local books, executable helpers, native authoring inputs, example hints and exact current-SDK bytes passed. Baseline bindings were recovered against prior sealed hashes; original process handles were reaped, without assuming PIDs remain unused forever. The mid-tilt and face-on native sheets passed delivery-legibility inspection only; Robert owns appearance acceptance. Performance remains deferred.
- **MOON/PLANET phase TECHNICAL_PASS / HUMAN_PENDING:** `celestial-phase-native/closure.json` seals the unit-sphere/night-side repair. Focused actual-caller 15,360 rows pass .002 with legacy and wrong-input rejection; 144 GPU + 525 library tests pass, with 2 performance / 1 existing timing probe ignored. Eleven identical exported Rust/canonical-SDK/WASM/packed-cartridge before/after cases cover 5,702,400 native pixels: zero failures/exclusions, maximum 1 / limit 2. All eleven cache/direct-footprint cases pass (.0002748763 / .002), and current-SDK WASM is byte-identical. GAS/RINGED/off control frames remain identical; dark MOON/PLANET bodies retain their occlusion rather than becoming transparent. Current published snippet, book/links, Rustdoc, inspector build and unchanged C/Zig declarations pass. No owned players remain. Both native review sheets are complete and legible; appearance remains Robert's gate. Phase angles, Size, textures, gain, other variants and packed fields are unchanged; older MOON/PLANET shading changes are documented. The obsolete scratch snippet and reference-helper prefix caused verifier-only failures, recovered without changing shader tests, images or tolerances.
- **End-to-end status reconciliation:** all 24 non-NOP opcodes exist, but inventory is not blanket verification. The former five unreconciled rows have been updated: LOBE now has a bounded technical closure; DECAL paint and backward-copy repairs have bounded technical closures, while its remaining authoring/composition coverage stays open; ATMOSPHERE and RAMP have sampled passing diagnostics pending evidence reconciliation; PORTAL paint and VORTEX phase now have bounded technical closure. Other rows retain explicit finite variant/domain/composition gaps. Remaining product work includes the showcase refresh, useful indoor/outdoor/hybrid compositions, affected integration/material/imported-state evidence and final documentation reconciliation, followed by human and clean-performance gates.


- `celestial-uv-native-recovery/closure.json`: **UV_CALLER TECHNICAL_PASS/HUMAN_PENDING**. Correct decoded MOON/PLANET/GAS/BINARY/SUN/RINGED semantics, eight identical Rust-export/canonical-SDK/WASM/cartridge before/after programs, **4,147,200 pixels, zero failures/exclusions, maximum 1 / limit 2**. All four textured-body cases now pass the original native-change discriminator; unrelated binary/SUN/absent-ring/off images remain byte-identical. Eight cache cases pass (**0.0003730683 / .002**), current-SDK WASM is byte-identical, local docs/builds/declarations pass; **142 GPU + 524 library passes**, two GPU performance and one existing timing probe deferred/ignored. The candidate kernel was restored byte-for-byte after bounded legacy replay; every source fingerprint matches the original GREEN suite. All owned players reaped; hygiene clean. A relink changed native EXE bytes despite identical source fingerprints: `relink-observation.json` retains this; native binary bit reproducibility is **not established or claimed**, and the rebuilt binary is separately source-bound and fully verified. No pixel, source, input or image-change gate was relaxed. Corrected native review panels are `moon_large-native-comparison.png` and `gas-native-comparison.png`; appearance remains Robert's gate. Hermes continues the independent ring-tilt audit; broader CELESTIAL/EPU and later performance acceptance remain open.


- `celestial-uv-footprint/`: **UV caller focused GREEN; full closure running**. Canonical GPU **36,744 / 49,152 → 0** at unchanged **.002** after the single caller change to `sin(angular_size_rad)`; maximum residual **0.0000025629997**, centre controls pass and restoring the per-pixel radius rejects the original 36,744 records. Eight exact exported guest/native before cases are frozen and reaped. The earlier baseline adapter used invalid opcode-31 blanks; exporter rejected them before any player ran, and `export-packing-red/` retains the failure. The adapter now reuses canonical blank-slot bytes. The angular fixture then differed only for MOON/PLANET/GAS front-facing textured samples (39/15/39): its **one UV-caller line** is explicitly aligned with the independently verified repair; every other fixture byte, ray, cosine, tolerance and negative control remains retained. Gain/phase/tilt/noise are unchanged. Hermes owns full GPU/host/native/cache/docs verification, cleanup and remaining slices; Robert owns visual acceptance. No user input is currently required. **Native closure rejected, not accepted:** all four labelled textured-body captures were identical because the native fixture misplaced high-word variant/colour bits and selected ECLIPSE. The unchanged-image discriminator caught this despite raw-byte parity. Original captures remain intact. `celestial-uv-native-recovery/` adds explicit decoded semantic-field assertions and correct inputs, then temporarily replays only the retained pre-UV kernel with finally-guarded exact candidate restoration in the authoritative checkout. It reuses the source-unchanged **142 GPU / 524 host** passes; it must re-prove all eight native/cache/current-SDK/docs cases without relaxing limits or image discriminators. No copied build tree or user decision.

- `celestial-authoring-audit/closure.json` seals the gain-only correction: **140 GPU + 524 library passes**, ten exact native/SDK/WASM cases, **5,184,000 pixels**, zero failures/exclusions, max **1 / 2**; cache max **0.000418874 / .002**. Full checks and current-SDK byte-identical guest build passed. The verifier’s incorrect comparison of SDK and GPU word orders is fixed and retained as a rejecting control; no cartridge bytes or tolerances changed. Owned players stopped; human review remains Robert’s.
- `celestial-angular-metric/closure.json`: **SIGNED-COSINE TECHNICAL_PASS/HUMAN_PENDING**. Four sites retain signed dot products; original canonical **270 / 3,120 → 0** at unchanged **.002**, exact legacy control still rejects 270 and front controls remain unchanged. **141 GPU + 524 library passes**, 2 GPU performance / 1 existing timing probe ignored. Nine identical real Rust-export/canonical-SDK/WASM/cartridge/native cases: **4,665,600 pixels, zero failures/exclusions, max 1 / 2**; front SUN, off and BAND images byte-identical. The erroneous behind-camera ring contribution is gone: after/ring_back is byte-identical to the off image. Other changed captures, wrong-direction/byte/layout/input controls, nine cache cases (**0.00048828125 / .002**), current-SDK byte-identical guest, local docs and unchanged declarations pass. All owned players reaped; hygiene clean. A copied-verifier typo (Cargo RED 101 accidentally rewritten as 91) is retained in `verify-exit-code-red.py` and corrected without changing any tests or captures. Gain/UV/phase/tilt remain held; broader CELESTIAL and overall EPU acceptance remain open. Hermes proceeds to the read-only UV-footprint audit; Robert owns appearance review and performance remains deferred.

## Showcase status snapshot — source/constant inventory, not visual acceptance

Fresh standalone Rust evaluation of the existing `epu-showcase` preset constants: 20 hero presets, 160 layer slots, 23 of 24 current non-NOP opcodes used. Missing from those preset programs: SCATTER_PHASED. There are 6 separate capability benchmark scenes. Record: `tmp/epu-review/remaining-control-audit/showcase-inventory.json`. This is compiled constant coverage, not a fresh showcase WASM/player/visual pass.

The existing showcase animation loop patches `param_d` for every configured moving layer; SCATTER_PHASED instead uses `param_c`, so adding its opcode also requires the correct guest phase route. The showcase source still advertises PORTAL/VORTEX as a reliable mover despite the read-only diagnostic showing no meaningful phase response. A current all-algorithm/variant gallery, migration of affected preset controls and human-reviewed composed environments remain unfinished. No new named opcode is committed to the implementation queue; multiband far-field hierarchy remains a candidate composition gap, not an approved new algorithm. Existing SCATTER_PHASED now occupies 0x18; older speculative mentions of using that slot are historical.

## Remaining-control audit — read-only evidence, not blanket acceptance

`tmp/epu-review/remaining-control-audit/result.json`: 16,384 production-evaluator GPU records, fixed .002 budget, unchanged sources. RAMP partition/paint and ATMOSPHERE gain-linearity probes report zero failures in their sampled controls. LOBE has 36 sampled decreases toward its axis; the explicit cap gate reduces every tested axis peak from ungated 2.0 to 0.5. DECAL reports 896 non-linear alpha rows and full-weight antipodal RECT/LINE copies. VORTEX remains visible in 944 samples but phase changes the image contribution by only 0.0000006556511: rotation of a circular SDF produces no meaningful motion. These identify specific remaining contracts, not permission to redesign every shape. Repair the DECAL paint contract with focused RED/GREEN; resolve unsupported motion guidance and existing shaping deliberately. Human appearance, broader controls/composition and deferred performance remain open.

## Prior checkpoint — BAND phase/depth technical pass; inventory continues

- `tmp/epu-review/band-phase/closure.json`: **138 GPU / 524 library tests passed**, zero failures. Two GPU performance probes and one existing tracker timing probe remain ignored; no performance acceptance.
- Applied Robert's explicit BAND decision: Color B alpha is depth 0..15 (zero plain ring; 15 retains the four-wave 40..100% profile), and phase is always `param_d / 256`, including zero. The original **31,104** phase rays and **.002** budget are unchanged: **4,536 → 0** failures; injected wrap jump rejects. Additional **73,728** depth/phase rows pass with nonzero modulation and an ignored-depth negative. Region, shape, cap fade and full-depth nonzero-phase profile remain preserved in the declared controls.
- Seven identical exported / canonical-SDK / actual-WASM / packed cartridges ran on archived-before and corrected players; plain phase-zero, full-depth phase-255 and GRID control images remain byte-identical. Nine actual guest frames cover held and skipped steps through phase **252,253,254,254,0,1,2,2,4**; every emitted byte and native state hash matches. Native memory rollback sync passes **264 advanced frames / check distance 2**. Combined **8,294,400 native/reference pixels, zero failures or exclusions, max 1 / limit 2**; wrong-byte and wrong-render controls reject. Seven cache/direct-footprint checks pass (max 0.00045571828185952956 / .002). Owned players are stopped.
- Real inspector depth/phase edits and disabled-at-zero/idle behavior preserve sibling bits. Existing API signatures and packed fields remain; additive host `band_radiance_with_depth(p, depth)` exposes depth without changing `BandRadianceParams`. API/guide/SDK/guest-inspector help, exact 256-phase ×16-depth helpers and their invalid-input guards, canonical C/Zig declarations, local book/link/Rustdoc/inspector WASM checks pass.
- Final hygiene removed one empty SDK rustdoc line and a guide EOF blank. Historical test/native/cache manifests are retained; `docs-cleanup.json` verifies the exact SDK old/new pair, every other executable source unchanged, unchanged public declarations, and **byte-identical actual guest recompilation** using the original WASM basename. The initial scratch LF copy and differing output-name custom section were rejected, not accepted as matching bytes. Current docs are built separately under `band-docs-final`; `git diff --check` passes.
- Review artifacts: `full_zero-native-comparison.png` is a static archived/current comparison; `band-native-wrap-trace.mp4` is a **nine-frame actual-native after trace**, 1008×740 H.264/yuv420p, 4 fps / 2.25 seconds, silent, fully decoded. No before video or full-cycle-loop claim: the optional before-trace attempt was rejected after a test relinked the player; seven original before stills remain intact. **Human acceptance remains Robert's.** Broader axis/basis/boundary coverage and whole-EPU acceptance remain OPEN; Hermes continues the ordered inventory. No commits, pushes, hosting, copied build trees or unrelated-service changes.

## Prior checkpoint — GRID technical pass


- `tmp/epu-review/grid-regular-loop/closure.json`: full GPU **136 passed / 0 failed / 2 performance probes ignored**; whole library **522 passed / 0 failed / 1 existing tracker timing probe ignored**. No performance acceptance is claimed.
- Applied Robert's explicit **regular and loop-safe GRID** decision without changing packed fields or API signatures. Repeat target rounds to whole 1..64 counts; checker uses nearest even 2..64. Low `param_c` nibble is 0..15 whole pattern cycles per `param_d / 256`; the checker cycle spans two cells. Count/rate migrations are intentional, not hidden compatibility claims. Hard line edges remain hard; cylindrical pole projection remains a documented limit, not a universal pole guarantee.
- Focused actual-GPU RED/GREEN: independent repeat/pattern oracle **57,509 → 0 failures / 262,144 records**; original chart rays with approved-pattern edge classification **10,294 → 0 / 65,536 scenarios, four offsets**; held/+1/+2 guest-phase comparison **22,043 → 0 / 368,640 records**, including **316 → 0** wrap failures. Fixed limits remain .002 temporal/oracle and .01 chart. Injected spatial and temporal jumps reject; hard-edge and nonconstant coverage checks pass. Original fractional-contract **12,713** RED remains in `grid-periodic/wrap-report.json`.
- **Seven identical exported/SDK/WASM/packed cartridges** ran before and after. Integral stationary stripes and WARPED_RADIAL control images are byte-identical. An additional actual guest executes the exact published phase helper through held/single/double steps across wrap; nine native emissions match all bytes and exporter state hashes. Memory-restore replay and **264 advanced native sync-test frames, check distance 2**, pass. Combined **4,147,200 native/reference pixels, zero failures/exclusions, max1/limit2**; discriminating wrong-byte/render controls reject. All owned players were reaped and final live-process inspection found none.
- Seven cache/direct-footprint cases pass, max **0.00041670845632180153 / .002**, no fold/corner exclusions. Real egui idle checks cover all scale bytes across named/fallback patterns without normalizing storage; actual pointer edits preserve sibling fields/nibbles. Native/API/guide/SDK and guest-inspector labels are aligned. Local book/link, full-phase helper + wrong-opcode, Rustdoc, inspector WASM and canonical C/Zig regeneration/check pass; public declarations remain unchanged.
- `grid-regular-loop/review.json` identifies two complete, readable, unresized native comparison sheets. These are static layout comparisons, not visual proof of temporal wrap; **HUMAN_PENDING**. The initial memory-rollback probe accidentally aliased live WASM memory. `counter-shared-snapshot-red.cjs` and its failed log are retained; copying the snapshot fixed the harness, with the same compiled guest passing. This was not a shader RED.
- Hermes continues the remaining per-algorithm continuity/source/runtime inventory before expressive expansion; BAND's phase-zero off sentinel is the next concrete source finding to probe, not yet a repair. Overall EPU acceptance and unreviewed visuals remain OPEN. No commits, pushes, hosting, copied build trees, worker agents or unrelated-service changes.

## Prior checkpoint — WARPED_RADIAL technical pass


- Source-bound receipt: `tmp/epu-review/cell-warped-native/closure.json`. Fresh full GPU **131 passed / 0 failed / 2 explicitly deferred performance checks ignored**; EPU host **119 passed / 0 failed**. The archived continuity RED and obsolete selector-6 fallback failure are superseded by this complete run; original failures remain retained.
- **22 exact Rust-export → canonical SDK → real WASM → packed cartridge → native-player cases** (14 retained regular cases, 8 WARPED_RADIAL cases). **11,404,800 pixels, zero failed/excluded pixels, max1 at limit2**; regular captures remain pixel-identical to the corrected-decoder baseline. Wrong-byte and discriminating wrong-render controls reject. All owned players exited, were reaped and passed the final live-process check.
- All **22 cache/direct-footprint cases** pass; max **0.0005166866393003788 / 0.002**, no fold/corner exclusions. Local book/API links, both exact 256-phase published drivers, Rustdoc, actual inspector WASM and SDK selector snippets pass. C/Zig declarations remain unchanged; generated whitespace was repaired at the SDK source and canonical generation/check passes. No public publishing.
- Organic CELL variant **6 / WARPED_RADIAL** coexists with regular variant **3**; variant 7 retains the fallback. The 128-bit layout and public declarations remain unchanged. Integrated public-route RED **67,304 failures → 0 / 207,360 records**; all-density chart **331,776** and pole **6,144** configurations pass with rejecting seam controls. Original geometry/ownership and decoder investigations are retained below.
- Human review: `cell-warped-native/review.json` identifies two archived-native/new-native comparison sheets, `warped_zero-native-comparison.png` and `warped_dense_gap-native-comparison.png`. Delivery inspection confirms intact readable panels and organic curved/oval motifs, but restored forms are **substantially coarser**. This is not artistic acceptance; Robert owns the verdict. Source/control difference is explicitly variant 3→6 plus the shared decoder repair. Regular RADIAL's separate shown acceptance is preserved.
- **New explicit authorization:** Robert selected **“Make GRID regular and loop-safe”** for the separate `GRID (0x09)` effect: whole repeat counts (even checker counts) and scroll control in whole pattern cycles. Old layouts and speeds may intentionally change; encoded fields and API signatures stay intact. The prior scale-choice blocker is resolved. Hermes now owns distinct spatial-wrap and guest-phase RED/GREEN checks, current native round-trips, authoring/docs updates and subsequent ordered work.
- Overall EPU technical/creative-control breadth and human acceptance remain OPEN; performance remains explicitly DEFERRED. No commits, pushes, copied build trees, hosting, workers or unrelated services. Historical status sentences below are capture-time evidence, not current ownership or active failures.

## Retained organic and decoder investigations — historical

- Recovered the exact original native-before CELL bytes by restoring their CRLF representation, not changing shader logic: `capture-before-cell.wgsl` matches the original receipt's SHA-256 `90c844fd5faf3f208844abe541bc8af83ebac63e3769548c826d6cd3731d7f17`. Original sources/captures remain retained. The two-case real-GPU scout preserves organic bands and nested eyes in the shared-phase experiment, but visibly exposes additional discontinuities; it is neither native guest parity nor human acceptance.
- Reproduced distinct causes: ownership/edge coordinates disagreed; geometry jitter jumped at cell-ID boundaries; angular warps lacked a consistent periodic winding. Shared-phase, smooth-jitter and periodic candidates remain recorded rather than being called fixed. Bounded relief detail is a separate provisional authoring choice, not falsely described as exact old-formula preservation.
- Found a shared oct-u16 precision defect while comparing direct and composed evaluation. Floating-point byte unfolding creates nonzero components on exact octahedron edges. Exhaustive actual-GPU RED: **7,189 / 65,536 directions fail**, including **511 directions / 512 nonzero edge components**. Integer-lattice folding passes all **65,536** against an independent double-precision oct-chart oracle; max component error **1.2392196513388853e-7**, frozen limit **2.5e-7**. Swapped-byte control rejects **65,278** directions. Readback preserves actual f32 bits as four exact byte-valued half channels; it does not mistake half-float quantization for decoder error. See `all-axis-report.json`.
- Integrated only that decoder root fix in `epu_common.wgsl` and its existing Rust inspector counterpart. Rust 2024 formatting now passes. Canonical decoder regression: RED **1 passed / 1 failed**, then integrated GREEN **2 passed / 0 failed**; EPU host suite **119 passed / 0 failed**. The first complete GPU run is retained as **122 passed / 1 failed / 2 deferred performance checks ignored** (`decode-full-gpu.log`), not relabeled as success.
- The remaining frozen SHATTER row 799 failure was a half-float test-predicate defect: exact-bit transport shows the original and corrected shaders both have a smooth largest-pair RGB difference above 0.01, but independent half-float rounding made the original appear below 0.01. `row799-report.json`, `row799-bits-report.json` and `continuity-reconciliation.md` preserve that distinction. Retained all rows, fourteen rays, nine controls, ownership/geometry checks and **0.01 output / 0.005 geometry limits**. Reused the already-established shrinking-distance/on-cut/equal-distance-same-side convergence gate instead of requiring every coarse finite gradient to be flat. The added constant-seam negative control passed while the unchanged old predicate still failed (**1 passed / 1 failed**, `decode-continuity-red`). No production CELL geometry was adjusted to fit this test.
- Prior source-bound decoder closure is verified in `tmp/epu-review/axis-decode-native/closure.json`: corrected continuity + constant-seam control **2 passed**; complete GPU suite **124 passed / 0 failed / 2 explicitly deferred performance checks ignored**, with unchanged before/after source guards. EPU host suite remains **119 passed / 0 failed**. Fresh exact Rust exports → canonical SDK → real WASM → packed cartridges → native player passed **14 cases / 7,257,600 pixels / zero failed or excluded pixels / max1, limit2**; wrong-byte and wrong-render controls rejected the mismatches. All **14 cache/direct footprint** cases passed, maximum error **0.00048196868615479893 / 0.002**, with no folds/corners excluded. All owned players exited and were reaped; the follow-up process check found no active native player.
- Before the organic integration, regular CELL/RADIAL production source was byte-identical to its protected snapshot; the new integration preserves the regular functions and branch while adding the separate variant. The shared decoder correction does cause small native-image changes against the archived pre-decoder captures (maximum channel change **3/255**, recorded separately from the passing current-source pixel-parity gate). Human appearance acceptance is not inferred. The prior regular-RADIAL closure below remains intact as historical source-bound evidence.
- Organic support/parity is now sealed on the actual GPU-buffer path (`buffer-control-seal.json`): **112,788 observed records**, zero RGB/region/continuity or direct/dispatched/composed failures under unchanged limits. The two locator families are accepted independently. A deliberately corrupted input row is rejected in BOTH halves; the injected centre seam is rejected at every observed record in BOTH halves. The original one-half negative receipt and earlier constant-input mismatch observations remain preserved, not reclassified as accepted evidence. This receipt qualifies the frozen candidate before integration; its production route now also passes the seven canonical GPU checks above. Native/export, complete regression, documentation and human appearance gates remain OPEN. The frozen GPU boundary gate also passed **331,776 chart cases** (all density bytes) and **6,144 pole configurations**, with rejecting chart and pole seam controls. The simpler unfused-wave reuse trial retained nine failures on the actual buffer path, so fused phase accumulation stays local to the new primitive rather than changing the shared wave used by existing algorithms.
- Historical unfused-wave failure attribution: the nine failures repeat one geometric witness (column 139, latitude input .13, maximum density) across material controls. Exact f32-bit `witness-trace.json` identifies final-scale phase/radius quantization amplified by the edge blend, with unchanged cell ownership. Dropping final re-normalization and sharing one ray centre both retained nine failures; restoring the old elevation-radius formula produced 21. Product/addition residual experiments retained those counts. These failed alternatives remain isolated and do not supersede the nine-failure baseline. Shared-helper arithmetic experiments also affect the legacy locator, so they cannot qualify frozen-locator preservation; the current candidate-local follow-up explicitly keeps those shared dependencies unchanged.
- These investigation checkpoints are superseded by the complete WARPED_RADIAL technical receipt above; human acceptance and the broader ordered goal remain open.

## Prior checkpoint — regular CELL/RADIAL technical closure


- RADIAL regular annular sectors, one centre cell per pole and filled-neighbour support are integrated and technically sealed. This replaces the earlier ownership/edge-distance discontinuity without changing the 128-bit layout or introducing engine time. `tmp/epu-review/cell-radial-regular/verified.json` is the retained pre-decoder-change source-bound receipt.
- At that frozen checkpoint, the complete GPU suite had **121 passed, 0 failed, 2 explicitly deferred cost probes ignored**. EPU host suite: **119 passed, 0 failed**. The 50 focused CELL checks and frozen fixture embedding checks remain part of the evidence, not a substitute for full regression.
- Exact Rust exports → canonical SDK → real WASM → packed cartridges → native player: **14 matched before/after cases; 7,257,600 pixels; zero failures/exclusions; max1/limit2**. The original `radial_zero` error123 pixel is cleared. All 28 before/after owned captures exited and were reaped; no owned player remains.
- Preserve the failed control receipt `native-pixels-antipodal-red.json`: `radial_wide_low` was nondiscriminating under direction reversal, not a packing failure. The fixed shared gate establishes that reference symmetry within the existing limit and renders deliberately inverted exported RGB bytes through the same GPU reference path; all control/parameter bytes and actual guest/camera inputs remain unchanged. The other 13 cases still reject the reversed direction. No tolerance was relaxed and no pixel was omitted.
- All **14 cache/direct footprint** cases passed (max **0.00048196868615479893**, limit **0.002**, no folds/corners excluded). Its wrong input explicitly changes axis and CELL paint, so symmetry cannot conceal it. Updated RADIAL API/guide contracts, local book links/build and both exact 256-phase published Rust drivers passed. The public site was not changed.
- Native visual comparison: `tmp/epu-review/cell-radial-regular/native-before-after.png`; two matched cases/four original-resolution panels with provenance. Robert's feedback on the shown regular result is “Ok it looks fine,” together with a request to restore the old organic behavior, fixed. Retain regular RADIAL; this does not approve every CELL case or close the organic-capability gap. The retained warped construction is in `cell-radial-regular/before-cell.wgsl`; the displayed original capture images remain hash-verified. That reference file is not asserted to be byte-identical to the entire shader in the native-before source receipt.
- SCATTER's requested finite exact-export/runtime closure is reconciled with `scatter-phased-final/verified.json`, not reopened by stale endpoint-animation notes. All **24 current opcodes** have ledger records. Historical mitigation guidance is explicitly non-normative.
- **Owner/next:** The next GRID record now has a genuine GPU RED (details below). Its scale-contract choice needs Robert’s decision before the GRID shader is changed; Hermes owns the subsequent repair and remaining engineering. The shown regular RADIAL result is acceptable to Robert; Hermes also owns the required organic-capability restoration, whose visual verdict remains Robert's. Broader algorithm/domain coverage, authoring/expressive/compatibility acceptance and overall human review remain OPEN. Performance remains a separate later clean-GPU gate. No commits, pushes, copied build trees, public hosting or unrelated process/service changes.

## Previous checkpoint — CELL cylindrical finite technical PASS

Main Astra MAX continues without subagents. No action is required from the user. The four failures in the previous 25/4 receipt are superseded by current-source GREEN, not waived.
- Root repair: BRICK now retains nearby filled rectangle fields, reusing HEX's fill-aware approach. The independent rectangle-union RED detected **2,176 failures**; GREEN covers **147,456 samples with zero failures**, while the missing-neighbour mutation still detects 2,176. Every historical endpoint ray remains; regular BRICK has explicit new owner/real-edge assertions. Existing raw-output convergence budgets remain unchanged.
- Current-source verification: **31/0** CELL-focused tests; **102/0** full EPU GPU tests, with only the two named cost probes deferred; **119/0** EPU host tests. These supersede the older full-suite counts below for this source checkpoint.
- Exact exported Rust → canonical real WASM → packed cartridge → owned player: six cases pass. Sealed captures match the direct production-shader reference over **3,110,400 pixels**, zero failures, maximum error **1** at tolerance **2**, no excluded pixels; wrong-direction references reject. The integral GRID and HEX controls are pixel-identical before/after.
- All six cache/direct solid-angle footprint checks pass unchanged **0.002** tolerance, maximum error **0.0004731121**; no folds/corners are excluded and all wrong-axis controls reject. All owned players are stopped and reaped.
- Historical local book/API links and both exact published drivers passed (256 phases each). Its GRID-rounding wording was overbroad and has since been corrected to the actual fractional GRID contract; a fresh documentation receipt is pending. Public site unchanged.
- Evidence: `tmp/epu-review/cell-periodic-closure/checkpoint.json`, `full-gpu-green.log`, `host-green.log`, `native-pixels.json`, `cache.json`, and `seal/receipt.json`. `finish.py` verifies current sources, binaries, artifacts, counts and player cleanup. Early parser/build-metadata failures and unsealed captures are retained, not used as acceptance evidence.
- Selected delivery image: `tmp/epu-review/cell-periodic-closure/sealed-review.png` (VORONOI, SHATTER, BRICK OUTLINE before/after). All panels/labels were checked for legibility only; **human appearance acceptance remains pending**.
- Next owner action: classify ordinary CELL ownership/support transitions and pole behavior, then continue neutral authoring, compatibility and expressive scene gates. The current checkpoint is not whole-CELL or whole-EPU acceptance. Performance remains deferred; no commits, pushes, hosting, unrelated services or pre-existing processes are touched.

## Previous checkpoint — APERTURE/MULTI technical pass

Main Astra MAX owns continuation; no subagents. This status supersedes older active-owner/status wording below. Whole goal remains WORKING; human visual acceptance is pending and performance remains deferred.

- MULTI now means regular N-by-N rectangular openings in one outer frame; N=1 is RECT. Seeded warp and ownership bridges were replaced under the user's consistency-first appearance waiver. Packed fields/opcodes are unchanged; the production shader is 60 lines shorter.
- Independent geometry RED: 25,670 failed samples → **zero of 61,440** after. Contour/sign checks retain their original limits; injected phantom zeros are rejected at 4,096 samples.
- **Whole EPU GPU: 97 passed / 0 failed / 2 explicit hardware-cost probes ignored. EPU host: 119 passed / 0 failed.** The current-source original stress rerun also passed 18 configurations; old isolated stress results are not current failures.
- Four identical exported Rust/canonical-SDK/WASM/packed-cartridge pairs: exact guest bytes/state; **2,073,600 native/reference pixels, zero failures/exclusions, maximum 1 under tolerance 2**. Wrong-byte/direction controls reject. All before/after owned players are stopped.
- **258,048 APERTURE sibling pixels** retain exact RGB/alpha/region bits. Cache/direct maximum error **0.0004440549456550258 < 0.002**. Local guide/API and both published helpers pass, 256 phases each; no public hosting.
- Receipt: `tmp/epu-review/aperture-multi-regular/verified.json`. Review image: `tmp/epu-review/aperture-multi-regular/native-before-after-labeled.png`; labels/openings are legible, not human-approved.
- Diagnostic exports are now opt-in, with numerical assertions outside export guards. The 90/7 and 94/3 setup-failure runs and intervening compile error are retained; only the final complete run is acceptance. Initially unguarded PRISM/planar diagnostics were refreshed by the first broad runs; subsequent guarded runs preserve their retained bytes.
- **Next technical owner:** main Astra continues fractional/polar CELL topology, then the broader authoring/expressiveness/contract ledger. **Human owner:** the user judges visual quality. No whole-goal completion claim.

## CELL uniform paint + periodic HEX: bounded technical pass, human review open

The owner explicitly chose quality/intuitiveness over old EPU scene appearance. Uniform Gap Alpha is now production policy: every opening, including wholly unfilled cells, obeys the same opacity. CELL returns straight color with one blend weight, removing double opacity. Neither policy changes geometric region ownership or the packed ABI.

The seam repair replaces rectangular-row HEX ownership with a regular periodic triangular lattice: density maps to 4..64 and rounds to whole columns, with both axes using that count. Filled polygon edge fields are composed, not hidden by chart-local fades. This deliberately changes HEX layouts. Reproduced direct/program occupancy differences also required integer mixing of canonical float input bits in scalar/vector CELL hashes; fractional seed inputs remain represented, and older seeded layouts intentionally change.

Evidence in `tmp/epu-review/cell-uniform/verified.json`:
- Paint RED: 4,237 failed channels; the first partial integration still had 190 direct/program parity failures, cleared by integer hashing. Final paint gate: 2,304 cases, 208 partial-region samples, zero failed channels at 0.002.
- Geometry RED: 6,039 continuity failures in the all-density gate; the prior local continuation also retained 75 ordinary-row field failures. Final HEX sweep: all 256 density bytes, 18,432 cases with observed straddles, zero failures at 0.01. The injected jump is rejected (55,296 failing fields). The chart audit now covers all 160 HEX rows, rather than only 32 historical same-owner rows.
- Final GPU command: `python tmp/epu-review/cell-uniform/check.py cell-release test --locked -p nethercore-zx --test epu_gpu cell_ -- --nocapture --test-threads=1` — 26 passed, 0 failed. Host `graphics::epu` subset — 68 passed, 0 failed.
- Five identical exported Rust/WASM/packed-cartridge cases before/after, matching guest state and exact bytes: 2,592,000 native/reference pixels, zero failures, maximum error 1 at tolerance 2, no excluded pixels. Wrong-byte and wrong-direction controls reject. All ten owned before/after players exited and were reaped; live PID checks passed.
- Local book/API links and both unchanged published helpers passed (256 phases each). Guide/API/CellParams document the new contract. The final verifier confirms transitive historical fixture bytes are embedded in the executed test binary.

Test migration is explicit: old scene identities/locations are not acceptance targets. All historical rays remain. Changed random placement invalidated the old >0.02 spatial-selection margin for some BRICK rays; positive interiors and the unchanged 0.005 geometry / 0.01 output budgets are still required. Rejected HEX prototypes run against the complete frozen pre-change CELL dependency chain, not current production helpers. No error tolerance was loosened and no rays were dropped.

Human review media: `tmp/epu-review/cell-uniform/native-before-after.png` (matched native frames; not a visual approval). The reused reference routine's historical terminal label says PRISM; the case inputs, raw records, and final CELL-scoped verifier establish the actual scope. The runtime wrapper label is corrected for future runs.

**Still open:** human visual acceptance, polar/other fractional CELL topology, APERTURE extended stress/contours, and the broader EPU plan. This is not whole-EPU acceptance. Clean performance remains deferred while the GPU is shared. Implementation owner resumes those technical gates; the user owns visual acceptance. Earlier CELL semantic-approval blockers below are historical and superseded by this entry.


## Current authorization override — consistency over legacy scenes

The user explicitly authorizes breaking older EPU scene appearances when correctness, quality or intuitive controls improve, and delegates that judgment to the owner. The owner chooses uniform CELL Gap Alpha for every opening; the earlier preserve-versus-uniform question is resolved, not an active blocker. Visible seams remain unacceptable; no prior preview is promoted to human acceptance. No cartridge/API layout change, commits, pushes, unrelated source changes or shared-GPU performance work is authorized by this appearance-policy override. Active work: behavioral RED/GREEN for uniform, once-applied paint opacity, then direct HEX geometry repair without compatibility fades. Earlier candidate/rejection records below are historical evidence, not current authorization limits.


**HIGH PRIORITY — discontinuity correctness:** TRAIL cutoff has user acceptance; FLOW field seams have bounded evidence, not blanket visual acceptance. The Status line below is the current execution owner; historical worker IDs elsewhere are not current status. The per-algorithm ledger gates continuity work.

Status: WORKING — APERTURE MULTI and PRISM fractional-wrap bounded technical acceptance are complete; human appearance gates remain OPEN. Main Astra MAX resumes the remaining CELL/HEX ledger and broader EPU plan. Performance remains DEFERRED while the GPU is shared. No whole-EPU completion or human acceptance is claimed.
Authoritative scope: `epu-correctness-and-open-authoring.md` in this directory.
Inspected source baseline: e7040c1e6938d8843d0b2123141c8423e41e554f. Candidate: current uncommitted owned changes; no commits or pushes.
Document review: gpt-6-astra / high, READY_WITH_CORRECTIONS; corrections incorporated.

### Worker completion reconciliation

`proc_bd802a55fd0f` supervisor ultimately reported idle_timeout / exit124, not clean completion. Its SCATTER/PATCHES artifacts were already recovered and independently checked by parent: `scatter-patches/verify.py` PASS and `parent-patches-integrate` three actual GPU tests PASS. Supervisor failure does not erase those bounded results or establish whole-task success. No duplicate retry; subsequent domain-boundary task owns the remaining finite checks. Prior recorded player cleanup applies only to receipt-identified owned players.

## Current owner continuation — CELL/HEX after PRISM technical closure

**Main Astra MAX owns the remaining CELL/HEX and broader-plan work.** PRISM is no longer blocked by its four off-cut preservation differences. Human appearance review for PRISM, MULTI and the broader EPU remains separate; performance remains deferred. No workers or subagents were launched.

### CELL/HEX continuation — candidate rejection and semantic decision

Production CELL remains byte-identical to the source bound to the isolated probes; its pre-existing dirty work is preserved. `cell-hex-fill-aware/result.json` records the rejected and fixed-owner candidates at 40 continuity failures each, versus zero for fill-aware endpoint geometry, with zero ordinary-control failures in all three runs. None of these candidates has been integrated.

The GPU-decoded Alpha-A sweep in `alpha-a-result.json` retains ordinary controls, identities and regional continuity, but changes 12 previously untouched opening-RGB records at each of Gap Alpha 0 and 7. The exact-return guard in `guarded-alpha-result.json` restores those opening colors but reintroduces 24 RGB continuity failures per setting. Alpha 15 remains green. These failures reject the attempted candidates; they do not prove that a preserving repair is impossible. Near-cut raw-distance changes are recorded separately from identity, region and color changes. The initial `alpha.py` changed Alpha B, not Alpha A; `alpha-field-diagnostic.json` explicitly excludes it as Alpha-A coverage.

Owner follow-up: `continuation-alpha.py` tried a local opening-paint continuation derived from the same clipped fields, rather than globally scaling empty cells. Its original Alpha 0/7/15 matrix passed, as did the three-seed wide-gap/support check. The systematic seeds 0–15 gate (`continuation-seeds.py`) then rejected it: at each of Alpha 0 and 7 there are **24 RGB continuity failures and 24 opening-color preservation failures**, with zero ordinary-control or identity failures; Alpha 15 remains green. `continuation-rejection.json` identifies seeds 4, 5 and 15, Fill 128, Gap Width 127/255 at the repair's x=0.5 support boundary. On those exact rays the original RGB jump is zero, the owner is unchanged and the region jump is zero; the candidate introduces the color step. The 0.01 budget is unchanged. This candidate is rejected, not integrated or visually submitted. These probes all exited; no native test player was launched during this follow-up. Production CELL still hashes to `fcea1fc4e0d9a2112a25502a9012d45466915e488cbfb289ae35c7ae844a40aa`. A failed local continuation is not proof that all preserving repairs are impossible; do not conceal the unresolved paint contract with another ad hoc fade.

Source contract discrepancy: CELL defines sky as both non-solid openings and inter-cell gaps (`04_cell.wgsl:401–405`), and the builder maps `CellParams.gap_alpha` to Alpha A, but the non-solid branch forces `w_sky = 1.0` while solid-cell gaps use `output_regions.sky * gap_alpha`. Applying Gap Alpha consistently to every opening is a proposed behavior change for existing CELL programs with unfilled cells and Alpha A below 15. No packed field or opcode would change, but the visible compatibility effect must not be silent. The owner needs the user's preferred contract before adopting that semantic change; preserving the current exception remains the alternative. Other independently authorized documentation and review delivery can continue. Broader EPU, human appearance, and deferred performance gates remain open.

### Closed PRISM fractional-wrap slice (human pending)

The original angular gate is behavioral RED/GREEN: **164 → 0 failures** with its unchanged `.005` region / `.01` RGB budgets. A no-op alias matched the baseline exactly; canonical-source replacement still reproduced the four off-cut differences in the earlier isolated candidate. The accepted repair preserves the original base weighting evaluation and returns it unchanged when continuation cannot contribute; only the affected wall blend is recomputed. Integral counts are not rounded or repurposed. PRISM's packed controls, public API and guest ownership are unchanged.

Production coverage retained **99,792 integral records and 48,384 off-cut records** exactly (the off-cut set includes integral cases; **24,192** are fractional-only). The persistent Cargo gate now also checks **20,664** unaffected region samples against the retained base on the same GPU/backend. The original probe body, input table, masks, trailing-layer order and tolerances stayed frozen. The prior shared CSV artifact was preserved and restored after each Cargo invocation.

The inherited fixtures retained 5,671 ordinary roots and passed 2,102 joins plus 350 cusps. An exact-module repeat was bit-identical; an injected constant jump produced 2,040 detected failures across 204 valid join rows without changing production. Exact counts and source hashes are in `tmp/epu-review/prism-offset-preserving/extended-result.json`. **258,048** SPLIT sibling pixels across all seven non-PRISM variants remained RGB/alpha/region-bit identical, with wrong-colour controls active in all **63** cases. The MULTI stress regression, three phased-SCATTER GPU regressions and 68 EPU host tests also passed after integration.

Five exact Rust exports (`narrow`, `medium`, `wide`, `oblique`, `integral`) went through canonical SDK → real WASM → packed cartridge → actual player before/after captures. Guest bytes, state hashes, ROM/WASM identities, camera and frame matched. **2,592,000** full-frame RGB pixels passed with **zero failures**, maximum error **1**, frozen tolerance **2**, no excluded pixels and active wrong-direction controls. The integral capture was pixel-identical before/after. All **15** owned before/after/seal player runs exited and were reaped. The subsequent CELL Cargo test relinked the shared player without source drift; a final native build and five fresh seal captures re-established current-binary provenance. All seal images are pixel-identical to the retained after captures, with the same full-frame error budget. Current player identity and both historical/current receipts remain in `verified.json` and `seal-pixels.json`.

Cache/direct-footprint checks passed all five 1,024-cell cases (maximum error **0.0004548926103937756**, limit **0.002**, folds/corners retained). Local guide/API generation, independent link/anchor checks and both 256-phase published drivers passed; obsolete PRISM-wrap limitation text was updated. No public site, unrelated service, commit or push was changed.

Evidence: `tmp/epu-review/prism-offset-preserving/verified.json` and the read-only `finalize.py`. Full native comparison: `native-before-after.png`; explicitly magnified diagnostic detail: `native-seam-detail.png`, with provenance in `media.json`. Delivery inspection found complete, legible panels and the localized seam change; **this is not human visual acceptance**. The inherited `square` negative-control name was corrected to `narrow` after the five before captures had already passed; only that failed negative-control stage resumed. Historical passing captures and receipts were retained.

## Closed technical slice — APERTURE MULTI (human pending)

- Read-back seal: `tmp/epu-review/aperture-multi-preserving/verified.json`, `APERTURE_MULTI_BOUNDED_TECHNICAL_PASS_HUMAN_PENDING`. The unchanged persistent 18-case test went from 17,212 continuity/shrink failures and 5,868 dispatch mismatches to zero. Production and candidate have identical non-comment tokens. No packed fields, IDs, public APIs, engine clock or hash family changed.
- Four actual inspector exports ran through canonical-SDK WASM, packed cartridges and native replay. All 2,073,600 current-reference pixels pass: zero failures, max error 1, fixed tolerance 2, zero excluded pixels. Subsequent test-target relinking changed the live executable without source drift, so all four guests were replayed against the final canonical player; these captures remain pixel-identical to the delivered pair. Capture player `26cc0f511aab15242e236e583a675f30051dedf1c0e56656e690158ed2bf7ca5`; sealed current player `fa12e4fc99f2349622a74c9ca193431bba0f4dfec20980dd72160937040ef770`. All twelve owned player runs exited and were reaped.
- Other APERTURE variants retain all 258,048 sampled GPU pixels bit-for-bit. Four cache/direct-footprint cases pass (maximum error 0.00048828125, limit 0.002, active opposite-axis controls, no excluded folds/corners). Three SCATTER GPU regressions and 68 EPU host tests pass. Local book/API builds and both published executable driver checks pass. `native-before-after-labeled.png` was delivered for human review; appearance is not automatically accepted.

- The isolated candidate passes the original and endpoint/axis stress matrices: 18 cases / 1,440 rows, zero .005 region/SDF or .01 RGB failures, zero dispatch-parity failures, and 34,415 opening/frame sign controls with zero flips. The first stress run completed and found a rounded outer-cell owner; clamping both ends then exposed different GPU hash results for constant versus floor-derived cell IDs. Only the upper owner is clamped in the retained candidate, keeping the ordinary floor-derived one-cell hash. Both rejected intermediate readbacks remain in `tmp/epu-review/aperture-multi-preserving/`.

- `contour-verified.json`: 294,912 finite-sign samples pass; 16,632 old-to-new and 16,622 new-to-old boundary witnesses pass the fixed 0.000002 position budget. Four endpoint windows also failed their own evaluator's crossing check; paired independent GPU roots resolve all four within the same budget, without exclusions, while preserving the original failed assertion and exact original bracket flags. No old zeros removed; 6,887 sampled zeros added only at existing geometric sign jumps, zero off-boundary additions. The shifted-field negative control fails 96,270 samples. Literal zero addition is disclosed, not historical pixel identity.

- Repair keeps each floor-owned warped piece and separately repairs opening and frame-level distance magnitudes; shifting one repaired nonlinear field would move authored frame contours. `upper-aperture.wgsl` is the source-bound candidate. Production has identical non-comment tokens, with untouched prefix/suffix and historical mixed line endings retained. Prior 1,218-failure RED, unchanged controls and the rejected 740-ownership-flip candidate remain historical evidence. Human appearance and shared-GPU performance gates remain separate and OPEN/DEFERRED respectively.

## Closed technical slice — SCATTER_PHASED and bottom-wall repair

- Current read-back seal: `tmp/epu-review/scatter-phased-final/verified.json`. Opt-in `0x18`, phase `param_c / 256`, depth `alpha_b / 15`; no new clock, controller, seed mutation, guest import, or packed-layout change. Existing `0x0A` remains static variation. Accepted single-fast motion is matched by real guest/native output; broader EPU appearance is not declared accepted.
- Current checks: 11 GPU tests passed (one explicitly deferred cost probe ignored), 119 EPU host tests passed; canonical SDK guests exercised static 8 / film 264 / controls 12 / bounds edits 8 / single 65 updates, repeated renders and copied-memory replay. Native 264-frame synchronization passed; all seven acceptance players exited and were reaped.
- 108 full-frame comparisons, 55,987,200 pixels, zero failures/exclusions, maximum error 2 at the frozen tolerance 2; 285 ownership captures, zero CITY star-leakage failures; four cache/direct cases passed with active wrong-mask controls. The source-matched legacy control preserves all 518,400 pixels exactly. The historical 4,913 one-value mismatch pre-existed this feature and remains archived rather than being hidden by a looser tolerance.
- Published phase setter and legacy endpoint helper were compiled/exercised; the actual setter ran through canonical SDK WASM with exact emitted-byte agreement. Wrong-opcode input was rejected. Local book (including final prose), Rustdoc and inspector WASM passed; no public site was changed. Performance remains deferred.

### Prior SCATTER diagnostics — historical receipts


- Latest directed preview: user requested a SINGLE SCATTER and faster twinkling, then clarified true full-on/full-off brightness rather than a persistent floor. `tmp/epu-review/scatter-single-fast/run.py` produced one active SCATTER plus seven NOPs, with no skyline or background layer, phase stride 4 at unchanged 30 fps, and a smooth pulse reaching exact 0 and 1. All 960 sampled GPU point coefficients reach both endpoints at the delivered cadence; loop/hold outputs are exact and disabling the sole layer produces black. The checked 256-frame H.264 clip is `scatter-single-fast/single-scatter-fast-full-range.mp4`. Human motion ACCEPTED by “great much better, please continue with the goal now”. This supersedes the brightness-floor candidate for this requested motion test, not the native wall-depth evidence. The preview itself made no production change; the subsequent continuation now owns the disclosed opt-in integration below. Full modulation must not retain a randomized nonzero floor. An informational density descriptor was corrected from C to A without changing rendered input or pixels; executed script and receipts retain that distinction.

- Human feedback rejected the RGB-endpoint recipe as visibly grouped/global pulsing. Retain its precision, byte, masking and replay results, but do not call that animation independently twinkling or accepted.
- `tmp/epu-review/scatter-individual/prototype.py`: isolated assembled-shader GPU candidate PASS, not a native guest feature. The existing point hash selects each point's phase, integer cycle rate and modulation depth; only brightness changes. No host clock, seed animation, geometry edits or production shader changes. Over the 256-phase clip the whole-field peak-to-peak/mean is **3.48%**, versus **65.37%** for the rejected native clip, against a frozen 15% discriminator. All 960 GPU point identities exceed the fixed 35% individual range requirement, with 202 distinct peak-phase bins. Phase 0/256 outputs are exact; the wrap step is 0.03515625 versus maximum interior step 0.04736328125. Per-frame opaque-CITY output stays unchanged.
- Measurement correction retained in `aperture-measurement-red.json`: a 5x5 image aperture can mix several nearby points. Its failed all-aperture amplitude claim was not fixed by relaxing the threshold; the same threshold was applied to actual GPU point identities. Whole-frame checks remain separate.
- Bottom marks were proven wall-to-floor ownership transitions, not star leakage. Only the CITY wall-depth byte changed, 150 → 255; roof/sky weights remain exact and the whole bottom strip becomes wall. `wall_native.py` PASS: actual inspector constants → canonical SDK → real WASM → packed cartridge → native player, eight exact byte/state observations, 518400 pixels, zero failures, max error 1 at tolerance 2. Owned player PID 48604 exited/reaped. This is a scene-authoring repair, not blanket SILHOUETTE seam closure.
- **Historical first integration snapshot, superseded by the closed technical slice above:** user feedback “great much better, please continue with the goal now” accepts the displayed motion; continuation uses the previously disclosed smallest opt-in SCATTER_PHASED proposal. `0x18` now selects the shared point kernel, `param_c / 256` controls phase and `alpha_b / 15` controls modulation depth. This adds an instruction, not a packed field or clock; older players treat this new instruction as NOP. Existing `0x0A` keeps its published static variation semantics. No separate native parameter wrapper was added: the existing EpuLayer/inspector/export route already carries all fields.
- Actual GPU TDD: `python tmp/epu-review/scatter-phased/check.py red test -p nethercore-zx --test epu_gpu phased_scatter_dispatch -- --nocapture --test-threads=1` exited 101 with dispatch maximum 0. The same check under `green` exited 0: 64 identity-following point rays × 257 phases pass dynamic dispatch, independent peaks, exact byte wrap, zero-depth/legacy equality and reserved-opcode controls. Source prefix/suffix outside the four-line shared-kernel edit remain byte-identical. Maximum accumulated point weight is 1.8554688 because nearby points may overlap; individual full-range coefficients require their separate acceptance check.
- SCATTER_PHASED first-candidate extended checks: 11 GPU passes, 1 intentionally ignored performance probe; 960 point coefficients reach exact 0/1 at +4 stride, with 55 peak bins and coefficient-ensemble swing 0.05535244 versus 2.0004885 for the rejected global-pulse control. Angular fine/on-cut error is 0.00048828125 under the unchanged 0.005 budget. Authoring phase/depth/packed-byte/JSON round-trip passes.
- Source-matched legacy control resolved the apparent regression without changing tolerance: `scatter-phased-baseline-control/legacy_control.py` (runner source: `scatter-phased/legacy_control.py`) rebuilt the actual player with all five frozen pre-integration shader/include inputs. That control reproduces the historical 4,913-pixel/max-1 difference, and is byte-identical to both integration captures (zero changed pixels). Source and native candidate restoration passed. Therefore the historical golden is not the current pre-change baseline; its failed assertion is retained, not waived. The constant-mode experiment made no difference and was removed. `capture-paired` uses the source-bound pre-integration image with the same exact-pixel assertion; `capture` retains the old historical test for reproduction. Final acceptance is rerun in `scatter-phased-final`.
- First real integration captures pass exact emitted bytes and native state hashes: static 8 updates, film 264, controls 12, independent bounds edits 8, single-layer 65. WASM memory replay and repeated-render checks pass. All six owned players exited. **Legacy preservation RED:** the archived exact-image assertion found 4,913 changed pixels, max RGB error 1; it was not waived. The generic shared kernel now receives explicit constant mode arguments from separate dispatch entry points, instead of testing the opcode inside each point. `scatter-phased-explicit` retains this fresh candidate separately and runs the unchanged exact legacy gate before repeating acceptance. Original integration evidence remains under `scatter-phased`; sync was not reached after that RED.
- Local documentation now explicitly says the legacy RGB helper is correlated endpoint modulation, not independent star twinkle, and explains the finite CITY wall depth. Current published helper compiled/ran for 256 phases; the local book build, 102 local references and 43 anchors passed. No public site was changed.
- Human motion acceptance applies to the latest single-SCATTER full-range clip, not blanket EPU appearance. The older two-layer prototype and the native wall-depth before/after do not establish whole-EPU visual acceptance. `scatter-individual/media.json` records the checked H.264 derivative (256 source frames, repeated to 512 encoded frames) and the separate native comparison. Existing broader correctness gates stay open; no performance claim is made.

## Retained SCATTER precision and correlated endpoint evidence

- Scope: existing SCATTER only; no new opcode, phase field, host clock, controller or cartridge format. Inspector now calls the packed high-nibble amount **brightness variation (static)**. All other bits remain preserved. A guest-side example uses existing RGB endpoints at different phases; seed, point identity and CITY geometry stay fixed.
- Actual shared-shader RED: 26 of 64 exact point centers were attenuated at the smallest radius (minimum core 0.413330078125, required minimum 0.999). Replaced the single shared `acos(dot)` distance with cross/dot `atan2`. Durable test: zero failures, retained old-formula control still fails 26 cases. Seven SCATTER GPU tests and seven rain/FLOW/TRAIL preservation tests pass; four inspector/packing checks pass. The guest-brightness capability test covers 64 points over 256 phases with exact geometry preservation and a peak-time spread; reseeding and globally synchronized brightness are negative controls.
- Original export refresh and source-bound canonical-SDK WASM captures are retained under `tmp/epu-review/scatter-authoring/`. The first CITY fixture mistakenly used FLOOR (`1`) instead of SKY (`4`); it is retained as a failed authoring control under `floor-mask-before/`, not claimed as an engine-mask bug. Pre-repair reference mismatches (7 compute / 12 raster pixels, maximum 4 bytes at a fixed limit of 2) remain recorded.
- Current exact native acceptance: six retained replay cases cover the original export, the wrong FLOOR authoring control, corrected static CITY/SKY, 264-frame film, held/+2 controls and independent CITY edits. Real WASM export/RGB-only/held/memory-replay checks pass; native sync advances264 frames at check_distance2 with no desync. Actual host state-hash samples match (32-sample logger cap is declared). No host configuration override. All seven receipt-identified players are stopped.
- Full native pixel gate: 29 uncropped960x540 captures, 15,033,600 compared pixels, zero failures, maximum1 sRGB byte at unchanged tolerance2; every wrong-direction control fails. All273 static/film/control captures retain opaque CITY ownership: 75,844,041 comparisons, zero leaks. Raising only CITY height preserves the192,496 sky pixels visible in both configurations within the same tolerance. The static ALL-region wrong-mask control still leaks23,795 pixels. Held phases and the native256-step wrap reproduce exact pixels.
- Actual shared cache/direct-source entry points also agree for the static export and phase128, over all1,024 octahedral cells and three channels in each case, including folds/corners. Maximum linear errors0.000148909 /0.000203210 at unchanged0.002; wrong-mask controls differ0.339880 /0.203174. The initial probe accidentally uploaded SDK hi/lo pairs as GPU word order; its inactive-control failure is retained in `cache-layout-before/` and was a fixture defect, not a renderer RED. Correct GPU marshalling uses lo-low/lo-high/hi-low/hi-high.
- `python tmp/epu-review/scatter-authoring/verify.py` sealed the runtime evidence with exit0 in `runtime-verified.json`, including re-reading every retained native case and recomputing selected pixel/ownership evidence. Subsequent SDK wording work is separately bounded: one doc comment clarifies the unchanged `twinkle_q` field and one guest-inspector label changes to `static var`. Their exact before/after bytes are preserved under `documentation-baseline/`; sealed runtime JSON receipts are not rewritten.
- Environment guide/API notes and the exact published brightness helper compile/check successfully; local mdBook generation and internal-link/anchor checks pass. Host Rustdoc (including its canonical-page redirect) and the guest-inspector WASM wording build pass. `python tmp/epu-review/scatter-authoring/verify_final.py` exits0: 304 native updates, 283 retained captures, exact renderer/ROM bindings and the sole documented SDK-comment hash delta pass; sealed receipts remain unchanged. Review derivative `scatter-city-guest-twinkle.mp4` contains512 decoded-verified frames at30fps from the native256-step cycle repeated twice. Basic encoded-frame legibility passed; twinkle naturalness, appearance, broader CITY/grove legibility and all human taste verdicts remain PENDING. No performance claim.

## Previously completed slice — loop-safe rain integration

- HUMAN_ACCEPTED: “amazing job on the loop safe rain! looks good please continue”. Only the two approved rain expressions changed: whole travel speeds `1.0 + floor(h.x * 2.0)` and circular distance `abs(fract(v01 - drop_pos + 0.5) - 0.5)`. No clock/controller, packed-field change or unrelated shader edit. Accepted MOTTLE/MASS, CRUST/support and rain-envelope code is preserved.
- Fresh current-source RED exit101: 1,794 phase-closure failures; five prior checks pass. GREEN exit0: all seven focused tests pass, including 4,096 column/phase +1/+2 travel and half-envelope-cap cases. Closure tolerance .005 and spatial/cap tolerance .001 are unchanged. Old fixed-coordinate cap rays run against the current envelope with only the historical motion restored; they are explicitly NOT a new-placement parity claim. Current travel/caps are exercised separately. The historical RED source/receipt remains retained.
- Exact current inspector export included through `include/zx/mod.rs`, compiled to WASM and packed once. Real WASM memory restore/replay and repeated render/held-input controls pass over264 ticks. Native replay emits the same128 bytes on every one of264 frames; the first32 actual host state hashes match (the existing logger is capped at32). No POST configuration injection. Native sync-test: check_distance2, 264 advanced frames, exit0, no desync; both owned players stopped.
- Fourteen original960x540 native captures, nine distinct phases, all7,257,600 pixels: zero failures vs current shader and the unchanged user-approved candidate program, maximum1 sRGB byte against fixed2; no excluded pixels, finite/opaque checks pass, and every wrong-direction control fails. Phase255→0 and +2 wrap are included; held/repeated phases reproduce byte-identical images. This is bounded approved-program parity, not universal visual acceptance or a performance benchmark.
- `python tmp/epu-review/rain-integrated/verify.py` exit0 rechecks source/build/ROM/WASM/capture identities, raw GPU readbacks, logs, declared counts and cleanup. Book/API motion guidance updated; local mdBook build and two exact rain notes /49 local references /19 anchors pass. Formatting and `git diff --check` pass. No commits, pushes, hosting, copied build trees, unrelated services or agents. Whole EPU goal remains open; main owner continues the remaining ledger slices, and human-only verdicts remain with the user. Performance stays deferred.

## Previously completed slice — shared carrier lattice repair

Owner: main Astra Max session; no spawned workers. Whole goal remains WORKING; performance remains DEFERRED.

- `epu_noise_hash31` now materializes its integer corner once for all callers. The prior float-expression path produced nonconverging jumps in MOTTLE/ADVECT/MASS carriers. Durable `shared_noise::shared_carrier_lattice_limits`: RED exit101 -> GREEN exit0, same 132 boundaries / 1,980 samples / 0.001 budget; raw final jump 0.005859375 -> 0, fBm 0.0019226074 -> 0.00048828125. Four existing PATCHES GPU regressions pass.
- Exact inspector exports were included through the canonical modular SDK, compiled to WASM, packed once, and replayed unchanged before/after. MOTTLE SOFT, ADVECT MIST/AXIS_CYL, MASS VEIL/AXIS_CYL and PATCHES BLOBS/AXIS_CYL passed all 128 bytes, nine guest camera/state emissions, and owned-player cleanup. Four current full-frame references: 2,073,600 pixels, zero exclusions/failures, maximum1 sRGB byte against fixed2; all wrong-direction controls rejected. PATCHES before/after is pixel-identical. Source-bound audit: `python tmp/epu-review/shared-noise/verify.py`.
- Large-scale pattern placement is retained in diagnostic inspection, but some local pixels change substantially (largest before/after RGB-byte deltas MOTTLE42, ADVECT94, MASS49). The 1-byte number above is current-player/reference agreement, NOT old/new appearance parity. HUMAN_ACCEPTED for the displayed MOTTLE and MASS afters: user said “both of the AFTERS look GREAT! this is the correct direction”. ADVECT was not shown and remains visually unreviewed; broader continuity/genre acceptance is not implied. Selected native captures: `tmp/epu-review/shared-noise/player-before-after.png`.
- Historical rain integration attempt: the whole-cycle candidate changed authored drop positions/speeds, so two old-location checks failed and the candidate was reverted pending a compatibility decision. That decision is now HUMAN_ACCEPTED and the bounded production integration below is complete. Historical RED/revert receipts remain preserved; they do not describe current production.

## Rain compatibility visual — historical preview, now accepted

- Following the user's request to see outputs before deciding, rendered a source-derived, isolated CURRENT/candidate comparison. Its inspector export fixes one RAIN_WALL/AXIS_CYL layer, approximately -Y axis, count, thickness, colors and all eight instructions; only phase advances. At that diagnostic stage production shaders and the player were left unchanged. The subsequent integration preserves the human-accepted MOTTLE/MASS repair.
- `python tmp/epu-review/rain-compatibility-visual/verify.py` exit0: all256 phase frames retained, original endpoint1794 failures -> candidate0 at unchanged .005, 512 sampled non-rain VEIL sibling outputs exactly equal, source/export/artifact hashes verified. Encoded H.264/yuv420p video: 960x334, 512 decoded frames at30fps, 17.066667s, no audio; complete decode and extracted-wrap review passed. Encoded phase255/0/1 inspection shows the current side relocating streaks at wrap, while the candidate retains positions with a small downward step. This is diagnostic observation, not human acceptance.
- Review files: `tmp/epu-review/rain-compatibility-visual/rain-phase64-before-after.png`, `rain-wrap-before-after.png`, and `rain-current-vs-loop-safe.mp4`. Left is historical CURRENT; right is the loop-safe candidate. These remain **GPU diagnostics, not guest captures**. User verdict: “amazing job on the loop safe rain! looks good please continue”. This authorizes the displayed motion/placement change. Current production, real-guest and rollback evidence is recorded in the integration section above; the original diagnostic receipts remain historical and unchanged.

## High-priority algorithm continuity ledger — mandatory visual correctness

Priority: repair unintended discontinuities before expressive expansion. Rules: `epu-correctness-and-open-authoring.md`, “High-priority visual correctness”. One algorithm = one verification record, with distinct variant/domain subchecks; reuse existing evidence, never infer blanket acceptance from suite totals. OPEN below means continuity evidence needs reconciliation, not that existing unrelated work failed.

Each record must accumulate: applicable boundary/domain coverage and justified N/A; exact test/RED/GREEN and tolerance; nonzero/core-preservation check; matched real-WASM capture/config/camera/frame/source identity; human visual verdict. GPU_PASS and RUNTIME_PASS are partial stages; VERIFIED requires all applicable checks plus human acceptance. Owner: parent Hermes. Current finite seam verification completes first; then reconcile existing PATCHES/SCATTER evidence and audit remaining evaluator boundary paths. No new framework or arbitrary render quota.

- **ADVECT** — bounded TECHNICAL_PASS across all six variants and three declared domains, retaining earlier shared-lattice evidence. `transport-chart-poles/closure.json` closes qualified shrinking pole limits, cap joins and exact off-cap preservation through 24 joint native cases. `carrier-temporal-loop/closure.json` closes cyclic shaping, packed-phase/control bridging and exact phase-zero shape preservation; joint evidence is 36 static + nine coupled actual wrap captures, 23,328,000 pixels, no failures/exclusions, max1/limit2, 36 cache cases, current-SDK equivalence and 264-frame rollback. The current suite is 163 GPU / 537 library passes; no engine-owned clock or new packed fields. Human appearance remains pending. This is the enumerated finite contract, not exhaustive parameter/composition/performance acceptance. Source: `nethercore-zx/shaders/epu/features/13_advect.wgsl`.
- **APERTURE** — MULTI bounded TECHNICAL_PASS / HUMAN_PENDING: regular N-by-N openings and one rectangular frame, N=1 RECT, independently verified zero set. Receipt: `tmp/epu-review/aperture-multi-regular/verified.json`: 61,440 geometry samples, whole EPU GPU 97/0 and host 119/0, 2,073,600 native pixels at max1/limit2, 258,048 exact sibling pixels, unchanged packed ABI. Former floor-owner distance bridges and their historical stress/contour status are superseded. BARS/simple-shape numerical gates remain; broader axes and human scene quality are separate.
- **ATMOSPHERE** — bounded TECHNICAL_PASS / HUMAN_PENDING, reconciled from `tmp/epu-review/atmosphere-ramp/closure.json`. The finite audit has 24,576 shared-GPU records across encoded variants, domains, gain and alpha/control cases; no shader defect was demonstrated. Joint RAMP/ATMOSPHERE runtime evidence contains 19 real-cartridge cases / 9,849,600 pixels, zero failures/exclusions, max1/limit2, cache/SDK/local-doc checks and owned-player cleanup. Inactive authoring fields were corrected rather than changing the evaluator. This is retained finite evidence, not new whole-family or artistic acceptance. Source: `nethercore-zx/shaders/epu/features/06_atmosphere.wgsl`.
- **BAND** — bounded phase/depth TECHNICAL_PASS / HUMAN_PENDING. `band-phase/closure.json`: frozen temporal RED 4,536 → 0 / 31,104 at .002; 73,728 depth/phase rows and rejecting controls; 138 GPU / 524 library passes; seven same-cartridge native before/after cases plus nine actual guest wrap frames and 264-frame rollback; 8,294,400 pixels, zero failures/exclusions, max1/limit2; seven cache and exact helper/SDK/editor/local-doc gates. Explicit alpha_b depth separates plain ring from cyclic phase, preserving packed/API signatures and adding the host convenience method. Review is a static native before/after plus an actual after-only slowed trace, not a fabricated before clip or full-loop proof. Broader axis/basis/boundaries and human verdict remain OPEN; Hermes owns the remaining technical work. Additional source-bound `remaining-poles/result.json` audit: 24 packed cases / 48 pole rows; 0 failures at `.005`; all shrinking ray displacements validated. Pole support passes this finite gate; it is not missing runtime/other-boundary acceptance.
- **CELESTIAL** — bounded TECHNICAL_PASS / HUMAN_PENDING for gain, signed angular visibility, authored-radius UVs, ring tilt and MOON/PLANET phase geometry. Receipts: `celestial-authoring-audit/closure.json`, `celestial-angular-metric/closure.json`, `celestial-uv-native-recovery/closure.json`, `celestial-ring-tilt/closure.json`, `celestial-phase-native/closure.json`. The latest phase gate includes 11 unchanged real-cartridge cases, 5,702,400 native pixels, zero failures/exclusions, max1/limit2, cache/current-SDK/local-doc checks and owned-player cleanup. This supersedes the old gain-only row; broader unenumerated basis/composition and human acceptance are not inferred. Source: `nethercore-zx/shaders/epu/features/08_celestial.wgsl`.
- **CELL** — bounded GPU_PASS + RUNTIME_PASS; human appearance OPEN. Latest source-bound seal: `cell-warped-native/closure.json`; complete 131-GPU/119-host regression, 22 exact packed-guest/native and cache cases, max1/limit2 native error without failed/excluded pixels. Includes the retained uniform paint, periodic HEX, GRID/BRICK and site support corrections, regular RADIAL polar cells and separate WARPED_RADIAL (6) with chart/pole/public-route controls. Regular captures match the corrected-decoder baseline exactly. API/guide/book/Rustdoc/SDK/inspector/helper gates pass. Coarser organic native comparisons await Robert; not whole-algorithm VERIFIED. Source: `nethercore-zx/shaders/epu/bounds/04_cell.wgsl`.
- **DECAL** — paint and facing repairs are bounded `SCOPED_TECHNICAL_PASS/HUMAN_PENDING`. Coverage is applied once; backward RECT/LINE copies are removed while front output stays exact. Real Rust/SDK/WASM/cartridge/native/cache and documentation evidence: `tmp/epu-review/decal-paint/` and `tmp/epu-review/decal-facing/`. LINE remains an unbounded front-chart stripe, without a new length or fade. Remaining authoring/control and broader composition coverage is not inferred from these repairs.
- **FLOW** — RUNTIME_PASS (bounded) — 12 scene boundary pairs + 771 lattice crossings, tolerance .001; `tmp/epu-review/showcase-seam/seam-parent-regression.log`. Parent capture/hash/cleanup audit passed; other pattern/domain coverage and human verdict open. Source: `nethercore-zx/shaders/epu/features/03_flow.wgsl`. Fresh exact-guest parity and review: `tmp/epu-review/seam-final-guest-result.md`; human acceptance pending. User reiterates pink/purple blob seams are a separate concern; no FLOW human approval given. Full-scene comparison required, not trail-only crops. Exact user-marked reference and five original/current crops retained at tmp/epu-review/showcase-seam/flow-human-review/user-examples.json. Marks are representative, NOT exhaustive; broader spatial/domain continuity stays open. Parent diagnostic inspection finds those five cuts absent in repaired crops; human acceptance still pending. Do not classify gray secondary FLOW coloration as a trail solely by appearance. STREAKS lane/turbulence repair is now bounded TECHNICAL_PASS / HUMAN_PENDING: `flow-streak-lanes/closure.json`. Registered unchanged tests: lane jumps 646 -> 0 (67,584 samples; max0.0008544922/.002), 2D lattice jumps 89 -> 0 (792 samples; max0/.001). Reuses the existing integer-corner hash and evaluates adjacent finite streak footprints with continuous colour mixing; no new opcode or controller. Eleven unchanged-cartridge native cases / 5,702,400 pixels pass (zero failures/exclusions, max1/limit2); NOISE and CAUSTIC controls are pixel-identical. Both human-approved Grove/Arcade frames are also byte-identical after current-player replay; all 24 owned before/after/control players were reaped. Two full unscaled comparison cards retain the changed STREAKS appearance for human review. The earlier ideal-axis locator pass remains explicitly unqualified; actual-evaluator decoded-axis brackets supplied the RED. This changes legacy STREAKS layout where unstable seeds/dropped cross-cell support mattered; existing accepted caustics are preserved. Full-scene refresh is now complete for all four affected existing recipes: Storm Front, Region Isolation, Projection Bay and Front Mass. `flow-streak-lanes/showcase/receipt.json` records original cartridges/current matching recipe bytes, native frame31, 32 guest updates, complete material views and four reaped players. These are integrated runtime/delivery captures, not independent full-scene pixel-reference acceptance; original scene receipts did not hash the retained replay files. Recipes were not retuned.
- **GRID** — bounded TECHNICAL_PASS / HUMAN_PENDING after explicit regular/loop-safe authorization. `grid-regular-loop/closure.json`: 136 GPU and 522 library passes, seven same-cartridge native before/after cases plus nine-step actual guest motion/readback and 264-frame native rollback sync. Combined 4,147,200 pixels, zero failures/exclusions, max1/limit2; integral-stripe and WARPED_RADIAL control captures remain identical. Seven cache cases, exact published helper, real editor input/storage checks, canonical bindings and local docs/inspector builds pass. Original fractional RED and corrected-contract RED remain retained. Nearest whole counts (checker even), whole pattern cycles, documented cylindrical pole limit; no hidden clock or packed/API signature change. Human static-layout/motion judgment remains open.
- **LOBE** — bounded TECHNICAL_PASS / HUMAN_PENDING (`lobe-centre/closure.json`): approved ordinary centre-bright profile; immutable GPU RED 3,150 → zero / 51,840, preserved palette/waveforms and rejecting old-cap control. Named waveform/cyclic-phase inspector controls preserve inactive/reserved storage and sibling bits. 146 GPU / 526 library passes; nine unchanged current-SDK/WASM/cartridge cases / 4,665,600 pixels, zero failures/exclusions, max1/limit2; cache, declarations, book/snippets/Rustdoc/inspector and owned-player cleanup pass. No whole-family/performance/human acceptance inferred. Source: `nethercore-zx/shaders/epu/features/10_lobe_radiance.wgsl`.
- **MASS** — bounded TECHNICAL_PASS across all four variants and three declared domains, retaining earlier shared-lattice evidence. `transport-chart-poles/closure.json` closes qualified shrinking pole limits, cap joins and exact off-cap preservation through 24 joint native cases. `carrier-temporal-loop/closure.json` closes cyclic shaping, packed-phase/control bridging and exact phase-zero shape preservation; joint evidence is 36 static + nine coupled actual wrap captures, 23,328,000 pixels, no failures/exclusions, max1/limit2, 36 cache cases, current-SDK equivalence and 264-frame rollback. The current suite is 163 GPU / 537 library passes; no engine-owned clock or new packed fields. The earlier shown shared-noise before/after remains HUMAN_ACCEPTED in that narrow scope; new carrier motion is HUMAN_PENDING. This is the enumerated finite contract, not exhaustive parameter/composition/performance acceptance. Source: `nethercore-zx/shaders/epu/features/15_mass.wgsl`.
- **MOTTLE** — bounded TECHNICAL_PASS for shared lattice and finite carrier phase/control scope. All four variants are unchanged native controls in `carrier-temporal-loop/closure.json`; qualified packed-phase, alpha/control, region and unused-domain invariance checks pass. Domain bits are unused (N/A), not untested cylindrical/polar paths. The 3D carrier has no angular chart pole. The earlier shown shared-noise before/after remains HUMAN_ACCEPTED in that narrow scope; new carrier motion is HUMAN_PENDING. Earlier lattice evidence remains `shared-noise/verification.json`; whole-family composition/taste acceptance is not inferred. Source: `nethercore-zx/shaders/epu/features/12_mottle.wgsl`.
- **PATCHES** — **RUNTIME_PASS (finite scope; human pending)**. Existing angular-wrap/lattice checks retained. New shrinking-pole RED found 120 smooth-variant CYL/POLAR failures; `patches-pole-closure` closes coordinates using the unchanged `advect_closed_azimuth` helper, without adding a coverage fade. Original coordinate formula, noise, seed, thresholds and retagging remain outside the cap. Focused GREEN: 240 pole rows, zero failures, all 240 on-cut jump controls rejected, 49,152 exact outside-cap comparisons, 11,706 changed inside-cap samples with nonzero coverage. Registered PATCHES suite 5/5, shared-helper 5/5, VEIL preservation 3/3. Rust export → SDK → unchanged WASM/cartridges → rebuilt native player: 14 cases covering all six variants on both charts plus direct/reserved controls; 7,257,600 pixels, zero failures/exclusions, maximum error 1 at limit 2. Both controls are pixel-identical. Full cache footprint: 14 cases / 14,336 cells, maximum error 0.00048828125 at .002; wrong-input controls reject. All 28 native players reaped. Local docs: five pages, 298 links, 155 anchors; published 256-phase driver and SDK/helper preservation pass. Region patterns and thus weighted opacity may change inside the coordinate cap; no new opacity/support gate is present. STATIC grain and max-sharpness threshold jumps remain intentional source-defined edges, not smoothed away. Historical `remaining-poles` RED is retained; current finite closure is `tmp/epu-review/patches-pole-closure/closure.json`. Human usefulness/appearance and broader ledger/performance gates remain open.
- **PLANE** — HEX/GRATING bounded TECHNICAL_PASS / HUMAN_PENDING, `plane-pattern-controls/closure.json`. HEX now uses regular nearest-site cells and primary-surface coverage; GRATING gap increases remove bars, with exact full/empty endpoints. Focused geometry 9,344→0 / 12,288 (`.002`) and gap controls 11→0 / 122,880 (`.005`); broad 159 GPU + 536 library passes. Fourteen unchanged canonical SDK/WASM/cartridge/native cases cover all eight variants plus gap endpoints/oblique controls: 7,257,600 pixels, zero failures/exclusions, max1/limit2; fourteen cache cases, local docs/FFI and exact approved-scene preservation pass. Six unaffected variants remain pixel-identical. Existing PAVEMENT/APERTURE support and authoring receipts are retained. Broader unenumerated normal/basis, phase/support and human hybrid-usability criteria are not inferred from this repair. Source: `nethercore-zx/shaders/epu/features/07_plane.wgsl`.
- **PORTAL** — SCOPED_TECHNICAL_PASS / HUMAN_PENDING — component paint, rough VORTEX motion/zero-roughness circle, reserved variants, real authoring/SDK/guest/native/cache/docs and rollback covered by `tmp/epu-review/portal-paint-vortex/closure.json`. All other variant geometries preserved; no blanket composed-environment or artistic acceptance.
- **RAMP** — bounded TECHNICAL_PASS / HUMAN_PENDING, reconciled from `tmp/epu-review/atmosphere-ramp/closure.json`. The finite shared-GPU audit covers 789,504 threshold/softness/axis records with partition, paint and rejecting-control checks. Joint RAMP/ATMOSPHERE runtime evidence contains 19 real-cartridge cases / 9,849,600 pixels, zero failures/exclusions, max1/limit2; original shader behavior was retained. Metadata/control, cache, canonical SDK and local-doc gates passed. Broader composition and human acceptance are not inferred. Source: `nethercore-zx/shaders/epu/bounds/00_ramp.wgsl`.
- **SCATTER** — requested exact-export/runtime refresh CLOSED in its recorded finite scope. Legacy `0x0A` retains static variation; the rejected globally correlated endpoint-animation recipe is historical, not the current twinkle implementation. Canonical SDK/WASM bytes, packed guest/native state, masking, replay and source-matched legacy pixels were verified by `scatter-authoring/verify_final.py` and `scatter-phased-final/verified.json`. The current SCATTER kernel and dispatch still match the final phase receipt. Other domains and whole-algorithm/human appearance claims remain open. Source: `nethercore-zx/shaders/epu/features/02_scatter.wgsl`.
- **SCATTER_PHASED** — bounded TECHNICAL_PASS; shown single-fast motion HUMAN_ACCEPTED. Opt-in `0x18`, guest phase `param_c / 256`, depth `alpha_b / 15`, unchanged 128-bit layout. Capture-time seal: `tmp/epu-review/scatter-phased-final/verified.json`; 11 GPU and 119 host passes, 108 native/reference comparisons over 55,987,200 pixels (zero failures/exclusions, max2/limit2), 285 ownership captures without leakage, 264-frame rollback, four cache cases and exact published-helper/local-book checks. All seven owned acceptance players were reaped. This is retained evidence, not a new run against every later shader change; broader compatibility, creative control and appearance gates remain open. Source: `nethercore-zx/shaders/epu/features/16_scatter_phased.wgsl`.
- **SECTOR** — bounded GPU / exact-export SDK-WASM / packed-native / pixel-reference TECHNICAL_PASS; human usability pending. BOX/TUNNEL/CAVE are covered by the shared 11-path packed wrap/endpoints/strength/domain-bit/axis audit (203,280 samples, max0.0009765625 at .001) and the existing authored-boundary controls. `tmp/epu-review/silhouette-paths/closure.json` seals the joint 15-case structural route: 7,776,000 pixels, no failed/excluded pixels, max1/limit2, all players reaped; 155 GPU + 536 library tests pass with timing probes skipped. No shader repair was needed. This replaces the old missing-runtime qualification, not a human verdict. Source: `nethercore-zx/shaders/epu/bounds/01_sector.wgsl`.
- **SILHOUETTE** — bounded TECHNICAL_PASS for all eight variants; the exact shown Grove repair is HUMAN_ACCEPTED, other diagnostic/composition appearance is not. `tmp/epu-review/silhouette-paths/closure.json`: joint 15-case exact Rust export/canonical SDK/WASM/packed-native route, 7,776,000 pixels, zero failures/exclusions, max1/limit2; inactive storage pixels are exact and every wrong-direction control rejects. Packed audit covers all eight profiles, three axes, byte corners, neutral/zero/partial strength and ignored domain bits (203,280 joint SECTOR/SILHOUETTE samples, max0.0009765625/.001). The 690 profile joins / 6,210 samples add mountain lattice, cell, periodic sine and spire-centre/antipode checks, shrinking/on-cut/same-side controls, nonflat ranges and 690 rejecting jump controls; final errors <=0.00024414063/.001. Latest broad regression: 155 GPU / 536 library passes. No shader or approved-composition edit was required; the original height-limit validation failure and corrected tiled probe are retained. Existing wall-depth/packed-octaves authoring fixes remain. No intrinsic animation phase or universal artistic/pole-limit claim. Source: `nethercore-zx/shaders/epu/bounds/02_silhouette.wgsl`.
- **SPLIT** — AUTHORING_PASS (bounded), continuity/human acceptance OPEN. Planar HALF/WEDGE/CORNER/CROSS/TIER/FACE: parent `split-planar-verify.py` passed 5,088 applicable fixed-axis rows, .005 region/.01 RGB limits; no production defect. BANDS: parent `split-bands-repair-audit.py` verified unchanged-test RED exit101 → GREEN exit0, 288 rows, reverse/center/quarter transitions, preserved minimum-width ownership and 1,720 selected original-profile controls. Local reverse-edge shaping now blends both edges through wall; overlapping broad shoulders visibly change. Four exact-export WASM/player captures passed byte/state identity and owned-command reaping (`split-bands-repair/runtime/candidate-receipt.json`); this is not matched pixel-reference continuity parity. Broad candidate still diagnostically shows no conspicuous cut; HUMAN_PENDING. Current-source BANDS and PRISM fixtures are now registered in `tests/epu_gpu.rs`; no worker is active on that historical follow-up. Runtime/pixel acceptance remains limited to the separately cited receipts. Other axes/basis, guest animation, runtime pixel-reference parity and human verdict remain OPEN. `tmp/epu-review/split-authoring-result.md` and fresh parent `split-authoring-parent` establish nominal blend-width display 0..0.2, all-byte semantic roundtrip, unchanged packing/export and width-only edit. Runtime retains minimum .001 AA width; metadata-only correction is not a visual repair. PRISM pole-cap repair now has bounded GPU_PASS/RUNTIME_PASS: parent `split-prism-cap-medium-audit.py` recovered timed-out worker evidence, unchanged .005 region/.01 RGB gates, exact off-cap parity and eight source/binary-bound exported-WASM runtime pairs. Caps finish at reachable endpoint and no longer inherit sector holes; cap appearance changes disclosed in guide. Matched `split-prism-cap/medium-runtime/cap-review.png` delivered for human review, verdict pending. The later fractional-wrap gate is closed by `tmp/epu-review/prism-offset-preserving/verified.json`: 164 original failures → 0 at unchanged .005 region/.01 RGB limits, five packed-guest cases / 2,592,000 native pixels with zero failures/exclusions and max1/limit2, plus 258,048 identical sibling pixels and current-player seal captures. The integral control is unchanged. This supersedes the old separate-GPU_RED label; broader unenumerated axes and human acceptance remain open. Source: `nethercore-zx/shaders/epu/bounds/03_split.wgsl`. **Packed-route gap now closed:** All eight SPLIT variants are included in `tmp/epu-review/split-surface-packed/closure.json` (21 combined cases, 10,886,400 pixels, zero failed/excluded, max 1 / limit 2; 21 matched cache checks). Domain bits are inactive and exact domain-control pairs passed. All variants contribute visibly against strength-zero controls. No shader/recipe change; this supersedes the old direct-WASM-only qualification, not the separate human gate.
- **SURFACE** — bounded GPU_PASS / RUNTIME_PASS / PIXEL_REFERENCE_PASS; HUMAN_PENDING. CRUST marked-corner repair integrated: all twelve frozen value probes passed before integration; current-source SURFACE tests 2 passed, native build passed. Unchanged exported DUSTED/CRUST WASM guests passed bytes/state replay; owned players reaped. DUSTED capture unchanged. Parent crust-integrated-pixels.py exit0: 518,400 CRUST pixels, zero failures/exclusions, max1 under fixed2-byte tolerance; wrong-direction control rejected at 474,917 pixels. Actual player before/after delivered; human corner-shading verdict pending. User authorized support-only fade between d=.05 cutoff and protected d=.1 interior. One-line alpha multiplier smoothstep(.05,.1,d); existing guard, material RGB, center suppression and ABI preserved. `surface-support-authorized-audit.py` parent exit0: original numerical RED weight .88916015625/RGB .61083984375; unchanged gates GREEN across encoded variants0..7 and both endpoints, final maxima .00048828125 weight/.00036621094 RGB; 2,880 off-band cases exact. `surface-runtime/verify.py` parent exit0: DUSTED/CRUST exact-export canonical SDK WASM, eight instructions/128 bytes, camera calls/native state and owned reaping verified; 1,036,800 full-frame pixels, zero failures/exclusions, max1 under frozen2 sRGB-byte limit, wrong-ray controls rejected. Direct WASM loading, not packed-cartridge acceptance. `surface-runtime/support-review.png` compares source-bound original/candidate GPU references, NOT old-player captures; diagnostic inspection sees narrow softened cutoff with matched distant material. Human visual verdict remains required. Other topology/phase/axis/composition gates OPEN; no performance claim. Source: `nethercore-zx/shaders/epu/features/14_surface.wgsl`. **Packed-route gap now closed:** All four SURFACE variants are included in `tmp/epu-review/split-surface-packed/closure.json` (21 combined cases, 10,886,400 pixels, zero failed/excluded, max 1 / limit 2; 21 matched cache checks). Domain bits are inactive and exact domain-control pairs passed. All variants contribute visibly against strength-zero controls. No shader/recipe change; this supersedes the old direct-WASM-only qualification, not the separate human gate.
- **TRACE** — bounded GPU_PASS / RUNTIME_PASS: angular wrap repaired across four variants/two periodic domains; parent independently reran 3 tests, maximum angular jump .0000038146973 <.005. Sixteen exact-export before/after runtime pairs audited; eight nonperiodic images unchanged. Recovered timeout recorded in tmp/epu-review/trace-wrap-result.md. Independent review read and integrated: tmp/epu-review/trace-wrap-independent-review.md, no blocker for bounded angular-wrap scope. DIRECT3D spatial boundary gate now bounded GPU_PASS / NO_DEFECT_IN_SCOPE: parent `trace-direct3d/verify.py` exit0, 122 validated boundaries/four variants, 5,002 RGBA pixels, final crossing/same-side max .0004883 < .005, flat-reference delta0 at readback precision; four retained guest pairs verified, no new runtime. Correction: the prior `trace_wrap_nonperiodic_reference_and_reserved` already contained basic DIRECT3D rows; this slice adds support/candidate boundary coverage, not first-ever reference coverage. No production repair. Human/pole/basis/region coverage not inferred. Additional source-bound `remaining-poles/result.json` audit: 96 packed cases / 192 pole rows; 15 failures at `.005`; all shrinking ray displacements validated. Failures are AXIS_POLAR opposite-pole meridian limits; mirroring support would change visibility and is not covered by VEIL-only authorization. No TRACE production edit.
- **VEIL** — RAIN_WALL bounded GPU_PASS / RUNTIME_PASS / PIXEL_REFERENCE_PASS / HUMAN_ACCEPTED motion. Original TRAIL cap repair remains accepted; its five fixed native-ray witnesses are retained as a historical-motion envelope control. The current helper now passes phase closure, all 4,096 column/byte-phase +1/+2 travel/cap cases, support/bar transitions and endpoint continuity at unchanged tolerances. Exact exported/canonical-SDK guest: 264 frame emissions, first32 actual host state hashes (existing logging cap), 14 captures / 9 unique phases; 7,257,600 pixels agree with current and user-approved programs, max1 against fixed2, no exclusions and all wrong-direction controls rejected. Held/repeated phases are byte-identical; native sync-test advances264 frames with check_distance2 and no desync. This accepts the demonstrated RAIN_WALL motion/cap repair, not all VEIL domains/variants or cross-GPU portability. `python tmp/epu-review/rain-integrated/verify.py`; other algorithm and performance gates remain open. **Authoring extension technically closed:** `veil-authoring/closure.json` adds truthful variant activity/count/width controls, 39 editor checks, 128 control/domain GPU cases, five-page executable documentation and exact rebuilt-player preservation without changing executable shader code. **AXIS_POLAR authorized support-envelope extension technically closed:** `veil-polar-boundary/closure.json` verifies pole/envelope RED 8/192 → zero, 33,792 exact outside-cap comparisons, ten unchanged-cartridge native cases / 5,184,000 pixels (max1/fixed2, no failures/exclusions), four exact front/non-polar controls, ten cache checks and published support documentation. All 20 capture players were reaped. Opposite-pole visibility changes were explicitly authorized; the shown cylindrical rain approval is preserved. No whole-EPU or new human appearance acceptance.

Inventory: 24 current shader metadata opcodes, including opt-in SCATTER_PHASED; inventory is not 24 verifications. Split genuinely distinct paths before closure.

## SCATTER/PATCHES angular and max-sharpness boundary evidence

- `tmp/epu-review/domain-boundary-result.md`: separate bounded GPU_PASS / NO_DEFECT_IN_SCOPE verdicts; 2 focused production-assembled GPU tests pass. Exact limits, intentional support/hard ownership, nonzero controls, one corrected finite-distance diagnostic assertion, portability gaps and remaining domains are explicit.
- Parent rerun: `python tmp/epu-review/domain-boundary/verify.py --gpu` from repository root. Uses serial GPU, required Cargo environment, fresh receipts and source/test-executable hashes. Final retained log: `tmp/epu-review/showcase-seam/domain-boundary-final.log`.
- No production change or new native runtime claim; prior runtime/human evidence remains unchanged. PATCHES integer-corner repair, FLOW examples and accepted trail repair preserved. No performance or expressive-expansion work.

## Mask neutrality — bounded guest integration VERIFIED

Final-source `mask-final-regression`: 24 correctness passed, 0 failed, 2 performance ignored. Rebuilt-player source switching/restoration also passed, unaffected viewport exact. `mask-guest/check.py` and `mask-guest/audit.py` independently rerun by parent: SKY and SKY|WALLS captures match exactly (RGBA tolerance 0) with nonzero fractional ownership 64/255; byte-rounding intervals exclude the old hidden boost. ABI program, source and player identities verified against retained receipt. All owned processes recorded stopped/reaped exit0. Background and finite dielectric response only: metallic interior is black, so do not infer reflection acceptance. See `tmp/epu-review/mask-guest-result.md`. The following paragraph records the earlier RED/build-lock history, superseded by this integration result.

Actual GPU RED: selecting SKY alone yielded0.3684082 at ownership0.25, while adding zero-weight WALLS yielded0.25. Shared `region_weight` now sums selected ownership without the hidden exponent. Focused real GPU test passes unchanged0.001 limit. This intentionally changes single-region soft-boundary rendering (weaker than old boosted output); packed layout, identifiers and multi-region semantics are unchanged. Not a silent compatibility promise: old authored single-region boundaries require visual review. Candidate not yet accepted through rebuilt guest/full regression. Native player exe is locked by the independent UI retry; no owned/pre-existing UI process was interrupted. Cargo compiled the test target but failed replacing player; explicitly running the updated test binary established RED/GREEN, not overall build success. Final cleanup removed now-unused counter/comments; final-source rebuild remains pending until UI releases player.

Existing compiled GPU test binary full run passed: 24 correctness tests, 0 failures, 2 performance probes ignored. This binary includes the semantic fix before dead-code/comment cleanup; final-source native rebuild and matched guest remain pending. Added explicit rendering-compatibility warning to API region-mask documentation; local mdBook build, generated HTML warning check and git diff --check passed. No publication.

### Prior source trace

Shared `epu_dispatch.wgsl` calls `region_weight`; `epu_common.wgsl` applies `pow(saturate(w), 0.72)` only for one selected region and plain saturated sum for multiple regions. Bounds paint (e.g. SECTOR/SILHOUETTE) consumes raw ownership weights, so painted ownership and feature strength cannot be assumed equal. This is an explicit existing semantic bias, not yet a newly reproduced rendering failure or authorization to silently change encoded behavior. Inspector search found region checkboxes/layer visibility, not a verified mask-only preview path. Next owner action after blend worker releases shared source: bounded matched-weight GPU reproduction and explicit compatibility decision under the plan; do not claim preview parity from source searches.

## Reserved opcode — REAL WASM BACKGROUND VERIFIED

`python tmp/epu-review/reserved-opcode/check.py --render` passes: guest appends all-ones instruction (reserved opcode31, all ancillary bits set) to exact neutral export. Baseline and changed-config background RGB pixels identical; real build/pack/player exit0, owned player stopped. No production change. Scope correction: this minimal guest and invalid-config guest draw backgrounds only, not material probes; reflection/cache behavior is not established by these two checks. Initial script adaptation failed before writing because the variable was TICK rather than TICKS; source was reread and adapted without changing preserved fixtures.

## Invalid guest config pointers — REAL WASM VERIFIED

`python tmp/epu-review/invalid-config/check.py --render` passed: canonical SDK guest first renders exact neutral export, then submits end-of-memory minus64 and u32::MAX pointers after rebinding the same valid config. Real host warns for both 128-byte reads (1114048 and4294967295 against memory1114112); baseline and rejection-frame RGB pixels exactly match. Offline build/pack/player exit0, owned player stopped, source/executable/artifact identities retained in invalid-config/receipt.json. Existing rejection mechanism works; no production edit. This closes bounded invalid-pointer behavior only, not every opcode/parameter validity condition. Blend authoring worker remains separately active.

## Signed normal orientation and rotated TBN — GPU VERIFIED

Extended the existing generated Mode2/3 material fixture with independently decoded BC5 normals and SH fields encoding signed X in red, Y in green and Z in blue. 8 identity-basis signed-axis cases pass unchanged 0.005 component budget; maximum observed component error 0.002936360. Added four production-packed +Y tangent cases spanning both handedness signs; `rotated-tbn` passes the same 0.005 budget in both material modes. Existing neutral-map/explicit-skip controls retained. `python tmp/epu-review/specular/run.py signed-normal-matched generated_material_normal_mapping_modes_2_and_3` exited0. First `signed-normal.log` failure was an oracle mismatch: subtracting another normal's ambient output assumed identical diffuse Fresnel gain. Same-sample channel ratios correctly cancel that gain; no production code or tolerance changed. This is a fixture correction, not a reproduced engine bug. Constant BC5 maps, identity/+Y tangent bases and both handedness signs are covered; arbitrary meshes and spatially varying maps remain outside this check. Full `rotated-tbn-regression` completed: 21 correctness tests passed, 0 failed, 2 performance probes ignored; `git diff --check` passed.

## Same-export material / normal control — REAL GUEST VERIFIED

Parent read guest.rs/check.py and reran `python check.py --render` in `tmp/epu-review/neutral-material`: offline WASM build, pack and native replay exited0; exact included neutral export hash unchanged; all owned processes stopped. Two960x540 captures: upper reference half exactly unchanged; lower matched-position normal controls changed with mean absolute RGB differences4.7939776,4.9396721,5.0747145 on byte channels. Four separate metal spheres visibly nonblank, narrow/broad roughness responses readable. This is real Mode2 normal-response evidence, not a numerical tangent-space correctness oracle, full dielectric/imported coherence or human visual acceptance. No production repair warranted by this diagnostic. Existing report `neutral-material-result.md`; refreshed exact identities/cleanup in `neutral-material/receipt.json`. Broader technical gates remain owner responsibility; clean performance remains deferred.

## Materials API and publishing source — VERIFIED

Traced `.github/workflows/docs.yml`: this checkout builds `docs/book` and uploads `docs/book/book` to GitHub Pages. No deployment or sibling checkout modified. Corrected `docs/book/src/api/materials.md` roughness-zero mirror claim and example comments; documented Mode2/3 finite exponent mappings and separate SH9 diffuse, matching `build_support/generator.rs` and shared lighting source. Existing local mdBook build passed; generated materials HTML assertions passed for both formulas, finite-lobe/SH9 limits and removal of stale mirror promises. `git diff --check` passed. This is local website-source verification, not live-site publication or whole documentation acceptance.

## Neutral composition candidate — machine verified, visual scope limited

Parent inspected author.rs, guest.rs and receipt, verified8 artifact hashes and reran actual-WASM ABI probe (3 render checks). Independently launched the same ROM through native rendered replay: exit0; fresh960x540 image pixel-identical to retained composition.png; owned player59648 stopped. Receipt `neutral-composition/parent-verification.json`. Source uses3 existing instructions (2 SILHOUETTE + APERTURE),5 NOPs, canonical SDK, exact export include. Diagnostic view shows a frame and overlapping tonal silhouettes; an architectural enclosure, recognizable exterior/style, depth/parallax and artistic quality are NOT established. This is a composition mechanism fixture, not broad environment acceptance. Do not add a new engine algorithm merely from this sparse art treatment. Next bounded real-guest check uses the same hash-frozen export with Mode2 material/normal probes in separate neutral-material root; worker proc_57d0687b6cb9, owner integrates on completion.

## Angular coverage extension — GREEN, normal incidence only

Extended the existing generated Mode2/3 fixed-world fixture without changing production, source colors, reference, or bounds: both ±Y former-switch thresholds at four azimuths plus ±Y/±Z pole approaches. `specular/angular-poles.log`:1 focused test passed;96 selected pairs; maximum residual jump0.000243669669 <=0.002, independent reference convergence0.000005502075 <=0.001. Uploaded N/V displacement gate unchanged at2e-5. No new production defect reproduced. Coverage remains normal incidence/white F0 with selected shininess values; controlled oblique-view continuity and human visual acceptance are not implied. Existing composition worker remains pending integration.

## Remaining EPU guide authority/material corrections — VERIFIED

EPU API/guide and active plan now reference canonical `include/zx/mod.rs`, not the legacy monolith. Render-mode guidance no longer promises an exact mirror at roughness0; documents finite s=1+255*(1-roughness), view-dependent EPU reflections, and SH9 diffuse. No SDK/ABI or runtime change. Existing mdBook build and git diff --check pass; generated EPU API/guide HTML contains canonical paths and omits legacy paths; generated material HTML contains the finite-lobe clarification and omits the mirror claim. Neutral composition worker remains pending integration; broader goal/human/performance gates remain open.

## Intermediate motion captures — VERIFIED FINITE REPLAY

`export-motion/phases.py` ran the same packed ROM through existing rendered replay; exit0, owned player64188 stopped. Five requested captures map replay frames0,59,119,120,179 to guest updates1,60,120,121,180 via the inspected executor lifecycle. All five decode at960x540 and are nonblank. Adjacent changed-pixel counts301378,422838,219817,0: scalar/detail and direction changes produce distinct frames; the color/intensity step persists unchanged at terminal hold. Exact hashes, mapping, and paths: `phases-receipt.json`; labeled sheet `motion-phases.png`. No host configuration writes or guest changes. Captures show parameter effects, NOT whole-object scaling, smoothness, artistic acceptance, or exact per-intermediate inspector readback. Earlier `--report` attempt correctly rejected rendered mode; JSON reports require headless execution. First indexing probe and cross-volume move failure are diagnostic setup failures, not engine RED.

## Inspector retry — terminal runtime parity VERIFIED

User requested retry after the desktop focus issue. A new owned player PID58400 was launched; no pre-existing process was closed. Background F4 worked; background mouse clicks did not. Foreground input then succeeded using screenshot-relative coordinates (the AX summary incorrectly suggested native coordinates). Enabled ZX EPU Tools; Env1 automatically loaded SCATTER with LOCK unchecked. Inspector showed motion_tick180. Fresh GET session asserted owned PID58400 and compared ALL eight layer dictionaries exactly with the original export plus the independently verified terminal driver changes: size188, direction32963, intensity192, color_a[80,220,120]; every other field/layer unchanged. No configuration injection used.

Receipt: `tmp/epu-review/export-motion/terminal-parity.json`; actual unlocked snapshot: `resume-session.json`. Captured `resume/captures/20260910-041610-terminal-motion-parity.png`, verified nonblank cyan render diagnostically. This is terminal-state parity, NOT isolated-phase motion/periodicity or human acceptance. Closed only the new owned player's window; `resume-owner.json` verifies stopped=true. Next: intermediate scalar/orientation/abrupt-change runtime/capture comparisons; broader plan and deferred performance remain open.

## Inspector authorization resumed — OS focus blocker (resolved above)

User approved reasonable verification with the explicit condition not to close any already-running processes. No pre-existing process was closed. Launched only a new owned motion player through bounded `export-motion/resume.py` (owner receipt `resume-owner.json`, task `proc_882d635fbe3b`). Background F4 succeeded: Debug Inspector shows motion_tick180. Background tab clicks did not select ZX EPU. Authorized foreground input and standalone raise both returned foreground_unavailable: Windows kept focus on Task Manager HWND33556592 rather than owned player HWND2163334. No mouse input was sent on the refused foreground calls; Task Manager was left untouched. Need user to focus a normal application, without closing anything, before continuing the Env1 readback. The owned wrapper has a finite cleanup deadline; only its own Popen handle is terminated at that deadline. Reopen a new owned player if the deadline expires before resumption.

## Export-derived scalar/orientation/step motion — ABI VERIFIED; runtime readback BLOCKED

Parent inspected `export-motion/src/lib.rs` and `probe.cjs`, then independently ran the Node probe against the compiled WASM and exact included inspector export. PASS:260 updates;261 exact128-byte configurations, repeated renders and fresh replays;1161 restored-memory replay comparisons;7 skipped-render checkpoints. Export hash59ce9e65cdff74ccdba199c89c92c2ffc1ff8f2c2f7b373eca876650ea7c68db; WASM hash8c2b5a8c8c305c4c42ac7a0156e8d017a768c6732846480e06ad93fc838d2a0a. Only SCATTER size/direction then intensity/primary color change; seed stays unchanged. This is actual WASM ABI evidence with inert graphics FFI, not GPU motion acceptance. Worker reports successful native sync260 frames; report `tmp/epu-review/export-motion-result.md`.

Worker's GET-only workbench attempt returned default NOP editor state, not the guest snapshot. Desktop F4 approval timed out with an explicit no-retry-without-user restriction. No bypass attempted. Runtime receipt records owned player stopped. **ACTION REQUIRED:** user authorizes reopening the owned test player and using F4/Env1 to load the actual guest state. Parent resumes runtime tick/config parity and isolated-phase captures after authorization. A terminal-phase load alone does not close all phase checks. Human visual, broader compatibility and deferred performance gates remain open; other independent evidence is preserved.

## Existing guest-owned cyclic animation — GREEN_VERIFIED

Parent inspected `guest-animation/{probe.cjs,run.py}` and independently reran `python tmp/epu-review/guest-animation/run.py`: unchanged showcase WASM built; ABI probe and actual native sync-test both exited0. Probe observed256 distinct128-byte EPU configurations over260 updates, FLOW param_d wrap255→0,260 repeated-render and fresh-replay matches,140 memory-restore replay matches, and final equality with intervening renders skipped. The Node ABI probe uses inert non-EPU imports; it is not graphics evidence. Separate actual player sync-test check-distance2 completed260 advanced input frames with no reported desync; owned player59060 exited naturally. No example or engine repair needed. Current receipts replace the worker-run receipts; worker report is historical execution evidence.

Finite limit: default cyclic FLOW byte emission and native Mode0 host rollback only. No visual periodicity, cache-pixel, Mode2/3 motion, exported-driver scalar/orientation/abrupt-change or human acceptance implied. Parent resumes those remaining named motion checks via active worker `proc_f202a9f6b120` (result: `tmp/epu-review/export-motion-result.md`), then independently integrates on completion; performance remains deferred.

## Architecture documentation audit — VERIFIED

Corrected `docs/book/src/architecture/epu-overview.md`: canonical modular SDK paths, procedural/imported source distinction, all existing MOTTLE/ADVECT/SURFACE/MASS entries, source-radiance purpose, procedural-only dirty-config skipping, and four-byte parameter layout. Removed a nonexistent reserved emissive field. Source checks: `layer.rs`, `runtime.rs::build_imported_envs`, `settings.rs`, and actual background/reflection WGSL. All24 documented opcode name/value pairs match the current enum; referenced source paths exist. Local mdBook build and built-HTML assertions PASS; `git diff --check` PASS. No ABI, algorithm, cache optimization or performance change. Plan handoff now identifies completed angular/UV work instead of restarting it. Guest-animation worker remains the next integration owner handoff.

## Current integration — continuity review and face-UV coverage

Fresh source-bound reflection review: **PASS_WITH_LIMITS**, `tmp/epu-review/angular-blend-review.md`. Weighted frame normalization is preserved; no blocking defect or relocated frame switch found. Existing finite angular coverage remains +Y/normal-incidence only; broader pole/oblique-view claims remain unverified. The doubled maximum fetch count still requires the explicitly deferred performance gate.

Added `nethercore-zx/tests/epu_gpu/imported_orientation.rs`: actual EpuRuntime slot3 import, six 32x32 RGBA16F faces with distinct horizontal/vertical gradients and face identity; 54 directions through production imported-background sampler and runtime radiance cache. Exact background RGB error0, matched cache-footprint error0.00037419796; unchanged bounds0.001/0.004. Initial `imported-orientation.log` failure0.0061035156 compared a filtered cache to an unfiltered point: **fixture mismatch, not production RED**. Corrected reference accounts for declared nine-tap source footprint, texel clamping and bilinear lookup; existing CPU oct decoding reused from irradiance tests. No production repair needed. Does not establish arbitrary textures, full background draw entrypoint, or human visual acceptance.

`specular/imported-orientation-regression.log`: **21 passed,0 failed,2 ignored**, no performance probes measured. Parent integrated angular review. Next active bounded worker: `proc_e479cd7a475a`, existing-example guest-owned update/replay verification; result `tmp/epu-review/guest-animation-result.md`. Parent resumes integration on completion. Broader expressive/authoring/docs/human/performance gates remain open.

## Earlier core and finite imported checks


User explicitly approved the paused formatting/regression command. Parent ran it successfully: **17 passed,0 failed**, `--test-threads=1 --skip cost::`; both performance probes excluded. Exact log: `tmp/epu-review/specular/ambient-full-regression.log`. No performance inference from test elapsed times.

Source-filter review retry used permitted direct read_file tools and returned **PASS_WITH_LIMITS** in `source-filter-review.md`; the earlier blocked review is superseded. Reviewer checked half-vector Jacobian, source-cell weighting/folds, single specular-color application, and bounded oracle isolation. No blanket imported/angular/human/performance acceptance.

Removed only the extra diffuse ambient specular-normalization gain from shared `blinnphong_common.wgsl`. The generated Mode2/3 regression reuses the existing material harness, black specular input and unit SH9 diffuse. Preserved actual Mode2 roughness/Fresnel blend and Mode3 convention. Initial fixture omitted the Mode2 blend; corrected oracle, restored original production gain, and reproduced fresh RED before repair: max error0.3524246515 against0.001. Same corrected test GREEN0.0002866522. Logs `ambient-contract-{red,green}.log`; earlier `ambient-{red,green}` are fixture diagnostics, not final acceptance. Actual same-ROM guest before/after in `ambient-guest/{before,after}.json`, both players stopped; `ambient-before-after.png` delivered to Telegram, no human verdict implied.

Next imported check now uses the actual EpuRuntime-produced texture view rather than test-written source pixels: constant colored six-face import into slot1, generated Mode2/3 response against independent BP references. `imported-constant.log`: all3 focused tests passed (reflection, ambient, imported constant). This is constant-radiance ingestion coverage, NOT directional face/orientation/seam coverage; no production change was needed for it.

Real imported WASM guest also exercised: `tmp/epu-review/imported-guest/{guest.rs,nether.toml,current.json}`. Canonical modular SDK, six explicit RGBA8 face textures loaded once with nonzero-handle assertions, `epu_textures` before geometry, all direct lights disabled. Three spheres use metal/roughness `(1,.1),(1,.9),(0,.5)`. Existing build/pack/player/workbench capture succeeded; current player stopped in cleanup. The actual frame shows all three probes, expected green -X background, colored sharp/broad reflection regions and diffuse shading. This establishes real guest import/render execution and handle checks, not directional numerical or human visual acceptance. No external assets, copied build trees or hosting.

Directional imported fixture prepared by worker, inspected and executed by parent. Added +Y normal before acceptance so top/bottom exchange is not hidden by XZ-plane symmetry. Actual EpuRuntime imports six distinct RGB16F constant faces into slot5; generated Mode2/3, N=+Z,+X,+Y, s=1,16,64,256 and NoV=1,.5,.1. Independent incoming-L cube-surface midpoint quadrature128/256 converged to0.0000055007884. GPU maxRGBerror0.00568017147 + full reserved0.001 =0.00668017147 <=0.01, zeroF0exact0. `specular/imported-directional.log`: all4 focused tests pass. No production repair needed. Constant-face directionality does not validate within-face UV rotation, arbitrary textures or angular continuity.

SDK docs: removed obsolete fixed bounds/feature slot ranges in actual generator input `include/zx/epu.rs`; clarified update()-owned animation and rollback-covered state there and in the book guide. Existing ffi-gen prefers the modular include/zx directory despite generated banners naming the monolith. Regenerated C/Zig, programmatically verified non-comment contents identical to prior files. `ffi-gen check --console zx` PASS; `ffi-gen verify --console zx` actually compiled both C and Zig to WASM. Local mdBook rebuild PASS using existing tools/output. No ABI changes. Cargo fetched existing locked generator dependencies; no added dependency/version changes.

Angular-continuity fixture executed by owner: RED max residual0.0039058020 against frozen0.002 (`specular/angular-continuity.log`). Replaced abrupt tangent-frame switching in shared `90_sampling.wgsl` with weighted overlapping Y/Z frames; weights vanish at their singular axes and sum >=1. Same128 samples/frame, up to256 fetches; increased cost explicitly DEFERRED, no performance/optimization claim. GREEN32 selected pairs: residual0.0000006364743; reference convergence0.000005502075. Full actual GPU run **20 passed,0 failed,2 performance ignored** (`specular/angular-full-regression.log`), unchanged2880-case/reference and imported directional tolerances. Rebuilt real imported guest reuses exact prior ROM/WASM, capture `imported-guest/angular-blended.json`; three visible probes, player stopped. Numerical pair coverage is positive-Y/normal incidence, not universal angular or human acceptance. Fresh source-bound Astra/high review is ACTIVE: proc_b83b993cffcc, bounded720s supervisor with completion notification, result `angular-blend-review.md`. Owner integrates/repairs on completion, then continues within-face orientation and neutral authoring/animation gates. Whole goal remains open.

## Source-filter repair — finite GPU GREEN; focused review completed

Worker proc_fc02dc4505b3 exited0 with partial helper/test edits but no result report. Parent inspected and finished directly. Actual production source-filter RED (`tmp/epu-review/specular/source-filter-red.log`): maxRGB0.0134701196 plus0.001 reference budget FAILS0.01. Replaced center/fold-stress smoothing with uniformly defined2x2 angular-cell average in `epu_compute_env.wgsl`; Jacobian `(sum(abs(unit_direction)))^3`, normalized per cell. Four source evaluations, unchanged128source resolution and128halfvector samples. No sample-count escalation or tolerance/reference changes.

Same GPU linear-helper isolation GREEN (`source-filter-2x2.log`): **2880 rendered cases,0 failures**, maxRGB0.0085025367 + reference0.001 =0.0095025367 <=0.01; zeroF0exactly0. Fixture injects analytic radiance only at evaluator boundary, runs production source-filter helper on GPU before saturation (P2peaks1.8), then generated Mode2/3 materials. Not a claim of full imported-ingestion quality.

Combined run initially found existing region test comparing a center point to the new angular-cell average. Parent made footprints match:4 actual direct-evaluator GPU samples/cell followed by independent CPU f64 Jacobian averaging, unchanged0.002 tolerance; removed old fold/corner exclusions. Maximum error0.00034815073. Final **16 nonperformance GPU tests passed,0 failed**, explicitly `--skip cost::`; log `source-filter-regressions.log`. Includes real EpuRuntime single/batch/import/grown source->SH9 paths, mixed-submission repair, normal maps, direct light, SCATTER and seams.

Real guest refreshed with current shaders: `specular-guest/source-filter-green.json`; identical WASM and ROM to selected before capture, player stopped. Local mdBook rebuilt using existing tmp/epu-doc-tools into existing tmp/epu-doc-book. Built architecture HTML verified to contain current BP contract and omit obsolete residual recipe; imported-background and cubemap-source descriptions corrected. No publishing/install/copied build tree.

Fresh read-only source-bound review active under proc_e10e0f34563f (Hermes default Astra/high, result `source-filter-review.md`). Owner resumes on notification. Still OPEN: review verdict, imported reflection ingestion/coherence, angular-motion/basis-switch stability, remaining material-dependent diffuse ambient_factor, subsequent authoring/expressive/documentation slices, human visual review, and deferred clean performance acceptance. This finite pass is not the whole goal.

## Historical recovery checkpoint — source-filter repair active; specular NOT accepted

Specular worker completed: original GPU RED maxRGB1.7591992 and zero-F0 leakage1.4619141; 128-sample candidate maxRGB0.01444668 plus reserved reference0.001 FAILS unchanged0.01 gate (six repeated cap observations among2880 rendered cases). Zero-F0 now exactly zero. `tmp/epu-review/specular/README.md` preserves residual evidence: source-cache bias0.01143169 dominates. Do not call this GREEN.

Matched actual WASM guest captures now exist in `tmp/epu-review/specular-guest/{before,candidate-failed-bound}.json`: same WASM/ROM SHA256, exact SCATTER export, metal roughness0.75, no direct lights, same light-facing camera. Selected `specular-diagnostic-pair.png` is diagnostic only. Original specular source was temporarily restored from captured dirty source, rendered, and candidate restored; no other repair reverted. Both players stopped. First camera faced away from the localized source and was not useful for reflection comparison; the selected pair uses camera+X. Git apply's line-ending normalization interrupted the first restoration assertion; subsequent matched pair completed with exact source restoration.

The interrupted source-filter process left no result, zero logged output, and no matching live process. Resumed bounded Hermes default-profile gpt-6-astra/high repair at proc_fc02dc4505b3, supervised max1200s/idle300s with completion notification. Inputs `tmp/epu-review/source-filter-task.md`, result `source-filter-result.md`, state `source-filter-resumed-state.json`. Owner resumes integration on completion. Scope: source texel area averaging and real shared-shader GPU verification; unchanged resolution, quadrature count, analytical reference and tolerance. No performance testing, commits or hosting. Goal remains open; human visual and deferred clean-performance acceptance are separate gates.

## Mixed-dispatch SH9 repair — RED/GREEN; specular implementation completed but rejected

The targeted SH9 repair review completed (`tmp/epu-review/sh9-repair-review.md`): narrow-source coverage repair confirmed, but mixed procedural/imported builds overwrite a shared irradiance count before one queue submission. Parent reproduced actual GPU RED: two procedural environments followed by one import left env1 c0=0 versus source reference5.941851. Compiler errors while preparing the fixture were not RED evidence.

Shared `dispatch_irrad_pass` now allocates an immutable 16-byte count uniform per encoded dispatch (existing BufferInit pattern); removed the unused persistent uniform allocation and queue parameter from all callers. No cartridge or shader binding contract changed. Same GPU regression GREEN: env0/env1 c0=5.941851, imported env2=11.136659, each exactly matches source extraction. Parent `cargo test --locked -p nethercore-zx --test epu_gpu irradiance -- --nocapture --test-threads=1`: **4 passed,0 failed**. Focused source review and integrated guest refresh remain open. Repeated procedural build calls in one submission are not covered by this mixed-path fix; their separate shared frame/ID buffers have not been claimed safe.

True half-vector Blinn–Phong CPU candidate: `tmp/epu-review/blinn-phong-quadrature.py` plus JSON, independent light-direction Gauss-Legendre reference512/1024. Declared absolute finite-case budget0.01. Includes constant, P2 and narrow5degreecaps, including caps aligned to64-point candidate samples.64 samples fails;128 samples worst0.004565, reference convergence0.000864. This is not arbitrary-input or GPU acceptance. Bounded production-shader/test implementation is active under proc_0a8d774fbe59 with completion notification; owner resumes integration. No performance timing/optimization or human aesthetic verdict.

## SH9 P1 coverage repair — GPU GREEN; targeted review completed

Fresh review `sh9-review.md` found a blocker: sparse64direction mip0 samples lose/overcount narrow bright sources. Parent reproduced realGPU RED for four5degreecaps with independent exact spherical-triangle texel-cell energy oracle and predeclared c0 tolerance0.001: +Z0vs0.0212923; alignedFibonacci0.1740104vs0.0200830 (error0.1539274); +X/-Z also missed. No tolerance loosening.

Replaced sparse directions with every mip0 texel, octahedral midpoint solid-angle Jacobian1/length(surface)^3, normalized4pi. GPU GREEN maximum capc0error0.00002108; all3SH9 tests pass; parent full nonperformance suite **14passed,0failed,2ignored**. Constant/P2 coefficient errors now <0.00044. This integrates finite source-cache texels, not unsampled procedural frequencies or exact higher-order hemispherical illumination. Serial O(map_size^2) perdirtyenv increases work; clean cost acceptance and any parallel-reduction optimization remain deferred, explicitly not measured/accepted.

Rebuilt currentplayer and re-used exact SH9 guest/export source: `tmp/epu-review/irradiance-guest/covered-source.json`; `covered-source/captures/20260910-005908-covered-source.png`. Compared actual earlier64direction frame with newframe:8608changedpixels, confinedsphere bbox405,195..555,345; visible change subtle. `sh9-coverage-before-after.png` is a labeled montage with sphere crops. Owned player stopped. No fabricated before image.

Targeted P1 repair review proc_cf6e2032f75c active, result `tmp/epu-review/sh9-repair-review.md`; its completion resumes owner integration. Previous review also found no additional blocker in directdiffuse or mipresize/importdirection wiring. Broader specularrepresentation/animation/breadth/human/performance gates stay open; overall goal WORKING.

## SH9 integration — parent GPU verified; fresh review active

Inspected Astra worker source/test/oracle and RED-declared/GREEN evidence. Parent removed obsolete `EPU_IRRAD_TARGET_SIZE`, `choose_irrad_mip_level` and its old selector test, synchronized source comments/book overview, then ran full non-performance epu_gpu suite: **13 passed, 0 failed, 2 ignored performance probes**. P2 c6=0.9936226 against analytic0.9960928; max coefficient leakage/error0.037428666 within predeclared0.04 (existing64-sample quadrature). Runtime/source-mip0 difference0 in five procedural/import/growth cases. Not arbitrary highfrequency/wholeSliceB acceptance.

Real minimal SH9 guest uses exact SCATTER export include with white dielectric sphere, all direct lights disabled, camera+X. Current player built, ROM booted, capture/receipt `tmp/epu-review/irradiance-guest/after.json`; source guest.rs and guest.nczx there. Image `after/captures/20260910-005341-after.png` shows bright lavender environment and dark sphere silhouette with faint purple rim; background looks+X while visible sphere normals point predominantly-X, so not a same-direction brightness comparison. Actual GPU before/after coefficients retained; no pre-fix material image was captured or invented. Owned player stopped.

Fresh read-only review of SH9, settings cleanup and direct diffuse gate change runs under proc_536453d78c1e; result `tmp/epu-review/sh9-review.md`, state `sh9-review-state.json`. Completion resumes owner assessment and bounded repair. Specular filter/BRDF representation remains OPEN after axial rejection, as do animation/authoring/breadth/docs/human gates. No performance work or user input requested.

## Documentation contract correction

Corrected stale material architecture claims in docs/architecture/ffi.md and docs/architecture/zx/rendering.md: Mode2 exponent is linear `1+255*(1-r)`, Mode3 normalized shininess is linear `1+255*v`; removed obsolete migration instructions claiming environment reflections were removed, an automatic sun was present, and obsolete specular-intensity semantics. Added normal-map slot3 with UV/tangent requirement. Source-bound checks against generator/snippets passed; these are current mappings, not a changed cartridge ABI. Full EPU docs semantics remain an open broader gate.

Reused existing tmp/epu-doc-tools/bin/mdbook.exe and mdbook-tabs; local book rebuilt GREEN to existing tmp/epu-doc-book, no installs/publishing/duplicate book tree. Architecture notes are source docs, not a claim they are individually published by mdBook. git diff --check passed. SH9 worker remains independent owner; completion resumes integration, not user approval.

## Direct-light recovery verified; SH9 worker active

Fresh Astra/Codex review returned no blocking findings for direct specular (`tmp/epu-review/direct-light-review.md`); limited helper/caller review, not full Slice B acceptance. Direct diffuse likewise had hidden material/roughness attenuation: actual GPU RED error 0.036059573, then GREEN 0.000048831105 at unchanged tolerance0.001. Shared Lambert wrapper now delegates without the gate. Both direct GPU regressions pass. Review extension to diffuse remains for the next integrated lighting review.

Real Mode3 rough-sphere guest captures: `tmp/epu-review/diffuse-light-guest/{before-rough,after-rough}.json` and `diffuse-before-after.png`. Guest source uses normalized shininess0; initial shininess1 captures had zero pixel change (smooth negative control, not evidence for roughness). Corrected before-rough was actually re-rendered with the original gate temporarily restored, then fixed source restored for after-rough. Same nether-reported payload hash307632e26bcbd3ce, frozen source/camera/light; cartridge byte hashes differ after repack. 17576 pixels change; both owned players stopped. Direct-specular images separately retained. No synthetic before images.

Axial BP approximation rejected for proposed0.01 full-angle budget: converged Gauss-Legendre reference (`tmp/epu-review/axial-kernel-reference.py`, runnable, with JSON) has convergence differences below0.000003; below-surface leakage reaches0.102567 despite exact constant gain. This is a CPU reference/model counterexample, not GPU acceptance or performance. Do not ship view-independent axial substitute as full BP or compensate artistically. Full specular representation/accuracy decision remains open.

Next live owner: Codex Astra high SH9 implementation, watchdog process `proc_05107c044e46`, state `tmp/epu-review/sh9-state.json`, final `sh9-result.md`. Owns only irradiance shader/runtime source binding and new irradiance GPU tests. Completion notification resumes parent verification and guest integration. No user input needed. Whole goal WORKING; performance deferred; authoring/animation/breadth/docs/human gates still open.

## Direct specular recovery — RED/GREEN; fresh review active

Worker deleg_34b63cc3 hit iteration cap without executing GPU checks. Owner rejected and reverted its unverified broad lighting/filter rewrite: mirror-direction exponent incorrectly identified with half-vector Blinn-Phong, cumulative mip filtering inconsistent with claimed independent source convolution, and insufficient runtime/reference tests. Earlier Slice A/shared evaluator/SDK/control changes preserved. No broad Slice B acceptance claimed.

Owner then replaced the broken worker test with one actual production helper GPU regression across five roughness values and dielectric/metal color gain at N=V=L. **RED_REPRODUCED** max absolute error 0.62197185 > fixed RGBA16F tolerance0.01. Root: direct_shell_compress multiplied the declared normalized BRDF by a hidden roughness/high-F0 gain. Removed only that gate from shared normalized_blinn_phong_specular; API/artist controls unchanged. **GREEN_VERIFIED/GPU_VERIFIED** max error0.0037341118. `cargo test --locked -p nethercore-zx --test epu_gpu -- --nocapture`: 10passed,0failed,2ignored (performance). Native subprocess environment reconstructed case-insensitively for TMP/TEMP/TMPDIR; simple bash exports had left linker temp-path failures. This is not whole BRDF or environment correctness.

Actual player before/after: `tmp/epu-review/direct-light-guest/{before,after}.json`, guest.rs/nether.toml/guest.nczx/capture.py and direct-specular-before-after.png. Identical static guest source, camera, mesh, roughness0.75, metallic1, light0.4, zero EPU, rim disabled; pack-reported WASM hash295a5cde6548e85f on both runs. Whole cartridge file hashes differ (repacked), so not byte-identical ROM evidence. Both 960x540 frames nonempty,3996 pixels changed around highlight; displayed difference subtle. Before captured prior to production edit, no fabricated baseline. Both owned players stopped. No performance run or aesthetic verdict.

Fresh source-bound Astra/high read-only review and next kernel recommendation running through watchdog proc_79446d29752a; final `tmp/epu-review/direct-light-review.md`. Completion notification resumes owner, no user action required. Full lighting model, SH/source separation, remaining contracts, animation, docs and creative-control acceptance remain WORKING.

## Active continuation — guest export closed; lighting implementation active

SCATTER export **RUNTIME_PARITY_VERIFIED**: exact included Rust const -> repo SDK `epu_set`/`draw_epu` -> real cartridge -> current player -> Env 1 snapshot loaded via F4 / ZX EPU / Enable Tools. GET session equals all eight exported JSON layers; editor unlocked throughout, no POST config injection. Capture 960x540 has 336172 nonblack pixels; visible diffuse lavender/pink field, not a claim of attractive rain. Guest camera now set in render(), because clear_frame resets matrices. The scene HTTP endpoint hardcodes Env 0 while ordinary guest procedural allocation starts at Env 1; earlier eight-NOP editor readback was not guest state. Existing UI snapshot selection closes parity without production changes. Earlier hardcoded build artifact path could pack stale WASM if CARGO_TARGET_DIR differed; verifier must bind both explicitly.

Receipt: `tmp/epu-review/export-guest/verification-receipt.json`; readback `runtime-session.json`; ROM `scatter-export-guest.nczx`; source `src/lib.rs`; capture `verified-workbench/captures/20260910-001332-verified-export-plus-x.png` under that same QA root. Owned player stopped. This completes only the static export subgate, not animation or overall authoring acceptance.

Lighting worker `deleg_34b63cc3` / `sa-0-011fc87b` owns bounded Slice B implementation and GPU regressions, excluding performance and guest/workbench. Native completion event resumes owner integration; owner must inspect decision/diff, run focused verification and obtain fresh shared-lighting review. No waiting on user for worker completion. Whole goal remains WORKING.

## What has actually happened

The initial supervised Astra implementation timed out (idle watchdog, empty stdout). It left a GPU test but no production edits or accepted result. Parent inspected the test, recovered a broken inherited Windows TMP path for native linking, and continued directly. A compiler/linker failure was NOT counted as RED.

Fresh production-shader GPU tests then reproduced two real defects on NVIDIA RTX 3080, Vulkan, driver 591.86. Before repairing them, the parent built the unchanged runtime, launched the existing WASM epu-showcase guest/workbench, captured real frames, and sampled limited process CPU/RSS and workbench request latency. Those are not GPU frame-time measurements.

After the minimal shared fixes, the same GPU tests passed, all generated shader variants built, and the same guest/player/configuration/camera was captured again. A fresh independent source review is in progress.

## EPU-01 — background and lighting disagree

State: RED_REPRODUCED -> GREEN_VERIFIED / GPU_VERIFIED for first-source and sequential-source cases away from existing fold smoothing. Full seam/compatibility acceptance pending.

**What was wrong:** The visible background treated a bounds layer's output as the new region ownership. Cached lighting merged it with previous ownership, including the initial all-sky default. A masked light could therefore illuminate objects even where the same procedural program suppressed it in the background.

**Actual RED:** first source cache/direct maximum channel difference **0.5136719**; sequential source **0.10888672**. Expected tolerance **0.002** for the sampled half-float outputs. The check executes production-assembled WGSL in actual compute pipelines, not a CPU copy of the formulas.

**Repair:** `evaluate_epu_layers` in `nethercore-zx/shaders/epu/epu_dispatch.wgsl` is now called by both `epu_compute_env.wgsl` and `common/20_environment/90_sampling.wgsl`. Preserve the old direct/background region contract; cache follows it. Do NOT redesign opcode modifiers or cartridge formats in this patch.

**Actual GREEN:** both cache/direct maximum errors **0.0**. First-source down-facing sky remains zero; sequential source peak remains **0.8461914**, so matching black outputs alone cannot make the sequential test pass. Existing fold/corner smoothing pixels are explicitly excluded; this is not an all-direction seam pass.

**Remote image:** `tmp/epu-review/slice-a-before-after.png` (delivered in Telegram). The sky is deliberately nearly black. Look at the enlarged sphere detail: the broad phantom halo disappears. Remaining direct/rim illumination is not claimed to be an EPU defect. This is a diagnostic, not an artistic environment proposal. The normal-map helper was restored in the same candidate; the separate shader test isolates the region mechanism.

## EPU-02 — normal maps were bypassed

State: RED_REPRODUCED -> GREEN_VERIFIED / GPU_VERIFIED for the shared BC5 helper and explicit skip. Full guest material/normal-map visual matrix pending.

**What was wrong:** `sample_normal_map` returned the base normal unconditionally as an old A/B diagnostic.

**Actual RED:** enabled mapping and explicit skip both returned **(0, 0, 1, 1)**.

**Repair:** restore existing linear BC5 sampling, remap XY, reconstruct positive Z, transform by TBN, and normalize. Keep the explicit skip flag intact. No new material model or artistic adjustment.

**Actual GREEN:** enabled mapping **(0.5996094, 0.003921509, 0.7998047, 1)**; explicit skip **(0, 0, 1, 1)**. Input is a real uploaded BC5 block with R=204/255, G=128/255. X/Z tolerance is **0.01** around the intended 0.6/0.8 direction. A real fragment pipeline executes the production shared helper.

## Commands and check scope

- `cargo test --locked -p nethercore-zx --test epu_gpu -- --nocapture`: actual RED had two failing behavioral tests; candidate GREEN **2 passed, 0 failed**. Former source-string test that asserted the faulty composition spelling was replaced by behavioral coverage, not retained as a false contract.
- `cargo test --locked -p nethercore-zx --lib graphics::epu -- --nocapture`: **68 passed, 0 failed** on candidate. These cover existing builders, packing, caches and settings, not visual correctness.
- Build emitted **72 generated shaders (+ environment + quad)** successfully. Actual GPU checks and guest capture are additional evidence, not inferred from compilation.
- `rustfmt --edition 2024 --config skip_children=true nethercore-zx/tests/epu_gpu.rs`: formatted only the new test.
- Windows recovery: invoke cargo through Python subprocess with a native `TMP`, `TEMP`, `TMPDIR` set to `C:/Users/rdave/AppData/Local/Temp`; shell-level export was overwritten by inherited tool state. Stop only the owned workbench player before rebuilding its Windows executable.

## Matched real-player capture / limited baseline

Evidence root (local, ignored QA files): `tmp/epu-review/`.
- Replayable configuration: `config.json`.
- Capture driver: `capture.py before|after`, existing localhost workbench port 4581. The driver verifies configuration readback and records player/ROM SHA-256, full session, camera and actual capture paths in `before-evidence.json` / `after-evidence.json`.
- Same epu-showcase cartridge, benchmark scene 1, camera angle 0/elevation 15, locked authored config, UI off, background and probe on, 960x540 capture.
- Original: `before/captures/20260909-204746-masked-region-before.png`.
- Candidate: `after/captures/20260909-205122-masked-region-after.png`.
- Matched enlarged crops in the remote sheet have unchanged brightness. No invented images or independently adjusted exposure.

**Measurements, NOT a speedup claim:** debug build, RTX 3080, 60 sequential workbench session samples with small gaps. Before: elapsed 4.1676s, process CPU 0.375s, RSS 424734720 bytes, median API response 15.9692ms. After: elapsed 3.4874s, CPU 0.21875s, RSS 468471808 bytes, median API response 5.8644ms. Scheduling, initialization and workbench serialization affect these values; this is neither a controlled EPU GPU benchmark nor integrated-GPU validation. API latency must not be presented as frame time.

Missing performance evidence: release EPU GPU timings, continuously animated/multi-environment frame-time distribution, representative older integrated GPU. The pre-change process baseline is limited, not full performance acceptance.

## Remaining work — NOT ACCEPTED YET

- Rest of Slice A: supported angular seams, bounds/modifier and opcode neutrality/invalid-value audit, docs/metadata agreement, broader compatibility/regression cases.
- Slice B: directional roughness filter, independent diffuse source, coherent BRDF/gains, removal of verified shell patches. No such lighting-model changes applied yet.
- Slice C: understandable existing inspector controls and guest-owned parameter animation/export/replay verification.
- Slice D: performance, imported/multi-environment/material integration and safe rollback verification.
- User visual/taste judgment: no verdict received or inferred.

No public page, remote port exposure, unrelated service changes, commits or pushes. Telegram attachments are the remote review surface. Baseline media retained until review, not treated as disposable output.

## EPU-05 — PATCHES wrap and smooth-noise discontinuities

State: RED_REPRODUCED → GREEN_VERIFIED / GPU_VERIFIED. Broader domains and independent review remain pending.

The GPU angular test compares nearly identical directions across cylindrical/polar wraps: five smooth variants, sixteen seed/elevation cases per domain, finite/nonconstant output, region difference <0.005. STATIC is intentionally discontinuous and excluded.

- RED angular difference: **1.0**. Circular embedding alone left **0.5761719**; tolerance was not relaxed.
- Separate RED lattice test at `(0.32,0.2,±0.000001)`: value noise -0.76708984 / -0.8125; FBM -0.5913086 / -0.74560547. Actual assertion <0.001 failed before hash repair.
- Repair: circular angular coordinates with circumference equal to scale; reuse existing `epu_noise_hash31` instead of PATCHES' separate sin-based hash. No added blur, noise library, or prescribed aesthetic.
- GREEN angular difference: **0.00048828125**. Lattice outputs match after RGBA16F readback: value noise -0.42944336, FBM -0.2841797 on both sides.
- Commands: `cargo test --locked -p nethercore-zx --test epu_gpu patches -- --nocapture`. Tests: `patches_angular_seam`, `patches_noise_is_continuous_across_lattice_boundary` (the latter was named `patches_noise_boundary_diagnostic` during its actual RED run).
- Full focused suite after formatting: **4 passed, 0 failed**; previous evaluator and BC5 checks remain green. No universal smoothness/cross-GPU identity claim.

Matched real player captures, same ROM/config/camera and 960×540 output:
- `tmp/epu-review/patches-before/captures/20260909-205915-angular-seam-before.png`
- `tmp/epu-review/patches-after/captures/20260909-210459-angular-seam-after.png`
- Config/readback/binary hashes: `tmp/epu-review/patches-before.json`, `patches-after.json`.
- Telegram sheet: `tmp/epu-review/patches-before-after.png`. Original angular patchwork becomes smooth at this view; no image blur added beyond identical resizing. Human creative verdict pending.

**Compatibility:** PATCHES seeded patterns change in every domain due to the reused hash. DIRECT3D coordinates, ABI and parameter meanings remain unchanged, but pixel-identical patterns are not preserved. Authoring-guide note added; existing content needs visual reinspection. The exact driver/compiler mechanism behind the old hash discontinuity is not established. Do not broaden hash replacements without reproduction. Owned capture player stopped.

SCATTER domains, remaining seams, invalid metadata and other plan gates stay open.

## Current recovery / next action

Parent owns integration after idle timeout. Read-only core review `deleg_a7d3a5f9` (gpt-5.6-luna) returned **core-ready**, not a verdict on all Slice A or PATCHES. Its cache-oracle suggestion is applied: both direct and cache are checked independently against the fixture expectation (first-source peaks 0/0; sequential-source peaks 0.8461914/0.8461914). Its unused `compose_bounds_regions` cleanup is applied after confirming no callers. Full GPU suite rerun: **4 passed, 0 failed**; generated shaders validated; `git diff --check` passed. These are review-strengthening/refactor checks, not newly claimed RED bugs.

The generated-material-path coverage gap is now GPU_VERIFIED for Mode 2/3 SH ambient response with normal-map enabled, neutral-map and explicit-skip controls. Guest visual/material matrix remains open. PATCHES is separately GPU-verified but not covered by the core review.

## Generated-material verification recovery

Parent integrated the worker test in `tests/epu_gpu.rs` and repaired its fixture, not production shaders: explicit `Vec<f32>` transform storage (untyped literals had uploaded f64 bytes), and an orthographic projection placing view-space z=-2 inside WebGPU clip depth. Disabled direct lights to isolate SH ambient. Fixed-pixel readback requires finite opaque coverage over the target; no brightest-pixel selection. Real generated `vs`/`fs` entrypoints, production vertex layout/bind groups, actual BC5 texture and production material lighting execute on RTX 3080/Vulkan.

Command: `cargo test --locked -p nethercore-zx --test epu_gpu -- --nocapture` with native Windows TMP/TEMP/TMPDIR as above.
- Mode 2 RGB: tilted map **0.03555298**, neutral **0.04446411**, explicit skip **0.04446411**.
- Mode 3 RGB: tilted map **0.117248535**, neutral **0.14648438**, explicit skip **0.14648438**.
- All alpha values **1**. For directional SH proportional to N.z, the tilted normal's expected response is approximately 0.8 of skip; acceptance ratio **0.7..0.9**, neutral/skip relative tolerance **0.005**. Both modes passed. This closes the generated-material wiring gap, not imported assets, direct lights, full material matrix, or human visual acceptance. Fixture failures are NOT a new production RED.
- Whole GPU suite at this recovery point (before the SCATTER repair below): **5 passed, 1 failed**. The remaining failure is the newly included SCATTER regression below; do not report the entire suite green.

## Rough-material lighting — reference definition active

Owner traced current sharedsource: reflection sampling90 uses roughness² LOD plus bias and additive residual; epu_compute_blur uses octahedral2x2box with foldside0.12weights; blinnphong_common ambientfactor and envFresnel/BRDF/shellcompress remain. These are sourceconfirmed paths, not yet new behavioralRED. Read-only worker deleg_0fe27f2f / sa-0-b66e2f1e is proposing one coherent BlinnPhong-compatible reference/filter convention and bounded tests. Mustrecord decision beforeproductionchanges; no performancework or blanket removal of explicitartistcontrols. Original roughlighting complaint stays a named acceptance target.

## SCATTER axis-relative distribution — RED reproduced

`scatter::scatter_angular_distribution_follows_axis` now actually runs. Cylindrical distribution rotation error **1.0**, nonempty peak **1.0**, violates **0.005** tolerance for matched local directions around rotated authored axes. It stops on this domain, so polar acceptance is not established. That was the actual pre-repair RED. Parent repaired `shaders/epu/features/02_scatter.wgsl`: angular cells are 2D; reconstruct their cylindrical height or polar radius into world-space directions around the authored axis before measuring angular point distance. Seed remains a field identifier instead of becoming a spatial Z coordinate. DIRECT3D and TANGENT_LOCAL branches, packed instruction fields and public API are unchanged.

State: **RED_REPRODUCED → GREEN_VERIFIED / GPU_VERIFIED** for sampled axis rotation, NOT whole SCATTER acceptance.
- Same regression/tolerance: cylindrical max error **0.00048828125**, polar **0.0**, nonempty peak **1.0** in both domains. No tolerance relaxation.
- `cargo test --locked -p nethercore-zx --test epu_gpu -- --nocapture`: **6 passed, 0 failed**, RTX 3080/Vulkan. All generated shaders built. `cargo build --locked -p nethercore-zx --bin nethercore-zx` and `git diff --check` passed.
- Actual matched player captures: `tmp/epu-review/scatter-before/captures/20260909-211720-axis-distribution-before.png` and `tmp/epu-review/scatter-after/captures/20260909-213646-axis-distribution-after.png`. Same existing epu-showcase cartridge path (not rebuilt), exact config, camera and 960×540 resolution. Candidate API config and effective view read back; one-shot view command fields become null after consumption, so verify scene/display state in session top-level and camera/lock in session.view.
- Config and binary hashes: `tmp/epu-review/scatter-before.json`, `scatter-after.json`. Before receipt lacks ROM hash; candidate receipt records it. No retrospective claim of a baseline ROM hash.
- Remote sheet: `tmp/epu-review/scatter-before-after.png`. Both frames are nonempty. Point layout changes and the conspicuous angular wedge in the original view is absent in the candidate view. No exposure changes or aesthetic verdict; static pictures do not establish rotation/seam correctness.
- Compatibility: angular-domain point layouts change deliberately; no pixel-identity promise for their old erroneous output. Existing DIRECT3D logic unchanged. No new controls, dependencies or artistic prescriptions. Guide caveat added.
- Remaining: angular wrap/cell boundaries, poles, TANGENT_LOCAL coordinate misuse, broader density/size/variant coverage and risk-focused review. The 3×3 local search with angular-size semantics still needs completeness checks at extreme settings; this patch does not claim to resolve them. No new performance acceptance from these captures. Owned capture player stopped; no worker remains active. Continue remaining Slice A before Slice B.

## Documentation coverage — REQUIRED / inventory started

User added explicit EPU docs cleanup including website/API. Initial local discovery found book API/guide/architecture pages, cheat-sheet references, include/zx/epu.rs and language bindings, shader metadata/generated inspector metadata, old macro-workbench and surface-expansion plans. Publishing source confirmed: `.github/workflows/docs.yml` builds `docs/book` with mdBook + mdbook-tabs and publishes the resulting book to GitHub Pages. This confirms the API site route, not every possible separate marketing site. Owner repaired API opcode table omission: source-metadata comparison RED missing0x17MASS → GREEN all23 non-NOP shader opcodes match plusNOP. File: docs/book/src/api/epu.md. No deployed-site claim.

Local docs build BUILD_VERIFIED: proc_fffa7621fbfd exited0; task-local mdbook0.5.4 + mdbook-tabs1.0.4 installed, toolcompile directory cleaned. Owner reran build after latestAPI/guide edits, exited0; exactgenerated api/epu.html and guides/epu-environments.html contain newhostMASS/controlcontent. Output tmp/epu-doc-book; tools tmp/epu-doc-tools/bin. No publishing. Owner subsequently reproduced brokenpublishedlocal links: three EPU pages pointed outside book to sibling designcatalog/SDKfiles. Removed unusablecataloghrefs, replaced them with explicit sourcecheckout references, repaired matching cheatsheet reference, and redirected obsoletecanonicalcatalog mentions to actualshader source. Rebuiltbook exited0; API/guide/architecture/cheatsheet generatedpages now have zero missing/escapinglocalhref filetargets. Evidence tmp/epu-review/epu-doc-link-check.json. Anchor/externalURL checks and broaderdocsemantic/exampleaudit remainOPEN. Guestboot worker deleg_72ccec12 / sa-0-ca97bafc is verifying exactexport in isolated tmp/epu-review/export-guest; owns no productionfiles, no workbenchconfigoverride allowed.

## Expressive capability audit — WORKING

Capability audit deleg_79f9d9ef returned; owner verified shader MASS opcode0x17 metadata and builder ending atsurface, with no MassParams/MassVariant search hits. **Confirmed authoring gap:** host-side typed MASS construction missing. MASS worker completed; owner inspected all three actual diffs (params.rs, builder.rs, mod.rs) and independently ran `cargo test --locked -p nethercore-zx --lib mass_builder_packs_nondefault_and_default -- --nocapture`: **1passed,0failed,506filtered**. Exact nondefault packedwords and EpuLayer comparison pass; default check proves encodedopcode only, not exhaustive default validation. Added MassVariant four existingvalues, MassParams, builder.mass, publicreexports. Shader/guestABI unchanged. Initial worker RED attempt hit unrelatedlink/temp error before missingAPI diagnostics, so do NOT claim observed behavioralRED or genuine compiledmissingAPIRED. This is an additive sourceverifiedAPI gap with successful packing verification. Website/API source now documents hostlibrary versusguestABI distinction, supported domains/ranges and lack of broadinputvalidation. NoGPU/visual/guest acceptance from this hosthelper. Worker mutation-warningfooter was contradicted by actual inspectedbuilderdiff; method/test present. Do not mislabel host helper as complete guest-facing authoring.

SCATTER inspector worker complete. Owner inspected actual editor.rs diff and independently reran `cargo test --locked -p nethercore-zx scatter_ --lib -- --nocapture`: 2passed,0failed,505filtered. Widgets now use density/variant-adjusted radius + RAIN/EMBERS falloff, highnibble twinkle steps preservingreservedbits, rawbyteseed. Owner disabled unusedWINDOWS twinkle. No shaders/packedABI changes; otheropcodepath unchanged. Initial workerRED was blocked bylinkerror, not behavioralRED. Owner rebuilt actual player, opened F4 via backgroundkeyboard, then backgroundmouse click failed toswitchZXEPUtab onfreshcapture. No focussteal/escalation while usermaybeworking. Mousecontrol/display acceptance remainsOPEN. Live workbenchAPI config→exportJSON/Rust→mutate→reload/readback verified all8layers and allpackedfields including nondefault SCATTER param_c0x35 reservedbits. Exact exportRust compiled with rustc and executableassertion passed. Evidence tmp/epu-review/inspector-controls/roundtrip-result.json and namedexports; not a guestboot or clickedslider claim. Ownedplayer PID52608 stopped. Temporarycompiledprobe removed; exports retained. Guide describes controls and remaining genericmetadata limitation.

Other audit outcomes remain leads, not accepted missing algorithms:
- Existing vocabulary already includes enclosure/openings, silhouette variants, region partition, regular/irregular patterns, local motifs, broad material/body/transport carriers. Names alone do not prove expressive quality.
- Historical MASS/ADVECT artistic failures show a useful diagnostic target; no fresh behavioral RED establishes the audit's proposed shared body-stage repair. Do not bake historical artistic prescriptions into runtime.
- Multi-band far-field hierarchy is a candidate gap. One height interval per SILHOUETTE operation is NOT proof that multiple layers cannot compose needed directional depth cues. First exercise existing layer composition/budget and controls against a small reused example before adding opcode0x18 or another family.
- No per-pixel world depth/parallax in a directional environment; distinguish that real representation boundary from feasible painted/abstract depth cues.
- Capability metadata/UI/guest construction consistency remains central. Preserve packed contracts; new reusable algorithms authorized when a demonstrated composition/control gap warrants them, not just because opcode capacity exists.

## PRIORITY CHANGE — performance deferred

User reports concurrently running AI image/video renders on the GPU. All preceding timing results are **contention-confounded observations**, not clean baselines or evidence of intrinsic production cost. Withdraw optimization priority based on them; preserve raw evidence, do not relabel measured values as invented or clean. Stop further benchmark runs. Worker deleg_0ad0cc65 instructed to stop and revert only its own candidate optimization delta; retain earlier correctness repairs. Owner subsequently inspected the shader diff and test module: only existing correctness repairs remain; test file restored to342lines/14051bytes with the four expected regressions and no optimization fixture. Worker applied no candidate shader edit. Its fixture attempt failed to link before GPU execution; that is not a test result or a correctness blocker for the restored source. Cleanup verified; no worker active, no timing rerun.

Priorities: supported behavior is predictable; metadata/controls truthfully expose capabilities; rough-material lighting is coherent; game developers can compose, isolate, edit, save/reload/export, and drive parameters deterministically from guest code. No prescribed weather/aesthetic systems, no forced abstraction or new editor. Authoring usefulness and human creative control are acceptance targets, not incidental polish. Deferred performance must not hold these up.

## Bounded hot-loop optimization — STOPPED / cleanup verified

Worker `deleg_0ad0cc65 / sa-0-222f26b9` owns only shared SCATTER shader and scatter GPU test module for ONE measured optimization attempt. Before editing: save small real production-GPU numeric output baseline spanning all7variants, unsaturated intensity/colors, seeds and moderate/max density. Require ≤0.001 absolute error on RGBA16F values, preserve existing correctness, then run same actual cache timestamp test. No size/quality caps, blur, artistic retuning or new algorithm/framework. Reject/revert candidate if no meaningful benefit or equivalence fails. Parent retains measured `cache-cost-owner.json` and actual image receipt `scatter-cell-after.json`, will inspect/rerun and capture matching player output if candidate retained. No production acceptance implied by dispatch; no player running.

## Actual radiance-cache timing — GPU measured

**Actual mip-0 cache pass: GPU_MEASURED, performance NOT accepted.** Owner inspected runtime-bound setup and reran `epu_cache_gpu_cost_probe` after worker completion. Uses actual EPU_COMPUTE_ENV/epu_build, storage instructions/active-ID buffer/frame uniforms, array texture, explicit dispatch counts. One environment, one white TANGENT_LOCAL BUBBLES layer, seed17, size255, twinkle high nibble8; moderate density code64 versus max255 and all-NOP black control. Actual128 default with16×16 workgroups;32 smoke with4×4. No production edits. Hardware timestamps cover compute pass including its stress resampling, excluding setup/CPU orchestration/SH/mips/readback/background/frame rendering.

Owner RTX3080/Vulkan591.86 run,1ns timestamp period, one warmup and5 samples each:
-32×32 moderate smoke median **0.467072ms**.
-128×128 moderate median **0.467872ms**; min/max0.467712/0.468928ms.
-128×128 max median **6.316384ms**; min/max6.154688/6.447904ms.
-128×128 NOP median **0.015424ms**; min/max0.015040/0.016032ms.

Readback finite/nonblack for scatter and black for NOP. Runtime bindings therefore exercised, though these controls do not prove every variant or scene correct. Source reviewed timestamp helper now accepts explicit workgroup counts; measurements are not the old32 dispatch mislabeled128. Similar moderate timings at32/128 are observed results, not extrapolated scaling. High case timing varies across runs (worker median6.688288ms); do not treat a single median as a hardware guarantee.

Command `cargo test --locked -p nethercore-zx --test epu_gpu epu_cache_gpu_cost_probe -- --ignored --nocapture`: **1 passed**,10filtered. Then default target: **9 passed,0failed,2ignored** (both timestamp probes intentionally opt-in). NativeTMP env as above. Evidence: `tmp/epu-review/cache-cost-owner.txt` and parsed4-case `cache-cost-owner.json`; preserve before forthcoming optimization. Existing visual preoptimization baseline `scatter-cell-after.json` retained. No player/worker active. Performance remains UNACCEPTED, especially for integratedGPU target; this is one layer/environment, not fullframe. Next bounded semantics-preserving cost reduction, then outstanding SliceA domains/contracts and later slices.

## Footprint bounds / microprobe results

Read-only review completed (deleg_2ac5624b): no real-arithmetic tangent support coverage defect identified; finite-precision outward conservatism remains unproved, and current tangent regressions cover DUST rather than all seven variants. Parent independently recomputed max-density/max-size rectangular candidate upper counts: STARS729, DUST2809, WINDOWS1600, BUBBLES4225, EMBERS3969, RAIN625, SNOW2304. These are loop counts, **not GPU timings or frame-rate predictions**. Existing size metadata mismatch remains queued.

**Hardware timestamp probe integrated and owner-rerun: GPU_MEASURED, performance NOT accepted.** Worker changed only root test module hookup and new ignored `tests/epu_gpu/cost.rs`; owner inspected entire probe and reran. It requests optional TIMESTAMP_QUERY, uses separate resolve/readback buffers, and timestamps compute pass only. Unsupported hardware prints UNSUPPORTED, not a measurement. Actual RTX3080/Vulkan driver591.86, timestamp period1ns; 32×32 samples, workgroup8×8, domain3, seed17, max size/intensity, varied positive-domain directions, all7variants at density codes255 and64. One warmup plus exactly5 measured samples per case; all14 cases finite/nonblack. Source uses compile-time variant/density constants (compiler specialization): this is a microprobe of production eval_scatter, NOT the full runtime dispatch/cache/SH/background cost or a frame-rate benchmark.

Owner-run median compute-pass times:
- STARS: code255 **0.150080 ms**; code64 **0.019104 ms**.
- DUST: code255 **0.556672 ms**; code64 **0.044928 ms**.
- WINDOWS: code255 **0.327328 ms**; code64 **0.031008 ms**.
- BUBBLES: code255 **0.850912 ms**; code64 **0.071616 ms**.
- EMBERS: code255 **0.804704 ms**; code64 **0.065248 ms**.
- RAIN: code255 **0.130912 ms**; code64 **0.016448 ms**.
- SNOW: code255 **0.439072 ms**; code64 **0.040192 ms**.

- Raw min/max/median and command output: `tmp/epu-review/scatter-cost-owner.txt`; parsed14 unique case results: `scatter-cost-owner.json`. Owner-run high/moderate BUBBLES ratio11.88× for this microprobe only. These are the first measured GPU costs for this surface, NOT retroactive pre-change baseline. Preserve them as the baseline for any forthcoming optimization.
- Explicit command `cargo test --locked -p nethercore-zx --test epu_gpu scatter_gpu_cost_probe -- --ignored --nocapture`: **1 passed**,9 correctness tests filtered. Then default full target: **9 passed,0 failed,1 ignored** (cost probe intentionally opt-in). Native TMP env as above; diff check passed. No production shaders changed in this step; no player/worker active.
- Remaining: real runtime/full-cache cost and representative hardware acceptance, footprint f32 boundaries/variant continuity (nonblack is not correctness), remaining domains/contracts and all later slices. Do not extrapolate these timings to full frame rates. No user action required.

## Tangent cell-boundary repair — bounded verification complete

**TANGENT_LOCAL cell continuity: RED_REPRODUCED → GREEN_VERIFIED / GPU_VERIFIED for the bounded regression.** Owner inspected worker test and reran RED: max error **0.19067383**, axis0xff80, local y boundary−0.5, x−0.1, domain weights **0.35400391 / 0.35424805**, nonempty RGB aggregate **96.24106**. Directions differ by ±0.00001 around the boundary, same 0.005 tolerance.

- Root cause: fixed 3×3 tangent neighborhood dropped contributing points as its cell set changed. Tangent search now derives conservative projected bounds from the spherical chord of the variant's angular support, plus half-cell jitter; includes RAIN/EMBERS extended support. It retains existing size semantics and does not blur, cap sizes or change other domains' search. Early return where tangent domain weight is zero. Larger size/density can cost materially more; no performance acceptance implied.
- Same GPU test GREEN: max error **0.00048828125**, aggregate **102.58859**, unchanged tolerance **0.005**. Includes decoded Y/X axes, x/y boundaries at−0.5/0/+0.5, varied companion coordinates, DUST/seed0/density code1/max size. This is not all settings or a reference-integral proof.
- Commands: `cargo test --locked -p nethercore-zx --test epu_gpu scatter_tangent_cell_boundary_continuity -- --nocapture` owner RED; `cargo test --locked -p nethercore-zx --test epu_gpu -- --nocapture` final **9 passed, 0 failed**, RTX3080/Vulkan. Native TMP env as above. Player build and diff check passed.
- Reused genuine pre-change capture/receipt `scatter-tangent-after.json` (binary match checked before editing), not reconstructed evidence. Candidate receipt `tmp/epu-review/scatter-cell-after.json`, frame `scatter-cell-after/captures/20260909-221543-tangent-cell-after.png`. Config/effective view, ROM SHA and resolution matched; binaries separately hashed. Sheet `tmp/epu-review/scatter-cell-before-after.png` delivered on Telegram.
- Visible major vertical seams disappear/reduce; an angular dark notch remains, whose cause is **UNCONFIRMED**, not automatically a defect or aesthetic failure. Both frames nonblack. White saturation limits visual sensitivity. Do not claim all artifacts gone. No new performance measurement; worst-case cost and representative hardware gate remain open. No player/worker active.
- Next: source-bound review of footprint bounds and measured cost before broadening this search to other domains; remaining angular local-search completeness/duplicate cells, poles, metadata/contracts, later slices and human review still open.

## TANGENT_LOCAL anchoring — bounded verification complete

**RED_REPRODUCED → GREEN_VERIFIED / GPU_VERIFIED for axis anchoring only.** Worker artifact inspected and owner reran RED: error **0.9940529**, Y peak **0.20422363**, X peak **1.0**, both nonempty, versus **0.005** tolerance.

- Root cause: world-XYZ projection plus seed was normalized as a point direction. Shared SCATTER now projects into a 2D orthographic tangent frame around the authored axis, hashes the field seed without spatial translation, and reconstructs the positive axis-facing hemisphere. Existing localization falloff and packed API unchanged. Only TANGENT_LOCAL behavior changes in this repair; seeded layouts and brightness change substantially and are not aesthetic acceptance.
- Exact same 64-case GPU regression GREEN: Y/X max error **0.00048828125**, both peaks **1.0**, nonempty RGB aggregates **160.04352 / 160.04205**. Matched local directions use axis dot 0.96–0.995 with varied density/seeds. Not all axes/variants/poles/cell boundaries.
- `cargo test --locked -p nethercore-zx --test epu_gpu scatter_tangent_distribution_follows_axis -- --nocapture` for owner RED, then `cargo test --locked -p nethercore-zx --test epu_gpu -- --nocapture`: **8 passed, 0 failed**, RTX3080/Vulkan, native TMP environment. Player build and `git diff --check` passed.
- Matched real captures: `tmp/epu-review/scatter-tangent-before/captures/20260909-220015-tangent-before.png` and `scatter-tangent-after/captures/20260909-220340-tangent-after.png` under same root. Receipts `scatter-tangent-before.json` / `scatter-tangent-after.json` retain config, view, session, binary/ROM SHA-256. Config/effective view and ROM hash matched, 960×540 showcase scene0, angle270°, elevation−10°, locked, UI/probe off.
- Telegram sheet `tmp/epu-review/scatter-tangent-before-after.png`: both frames nonblack. With deliberately maximal size, the candidate redistributes bright areas around the tangent frame, but **sharp cuts remain visible**. This is NOT artifact-free visual acceptance; investigate cell-neighborhood completeness separately before claiming field correctness. No new GPU performance acceptance. Owned player stopped; no worker active.
- Domain3 remains a supported local anchored frame; historical weather/style targets are not adopted as requirements. Next: reproduce remaining visible cell cuts, low-density duplicate neighbors, poles and remaining metadata/contracts. Whole-plan acceptance remains open.

## SCATTER wrap verification / source audit

Worker test integrated and owner-verified; no worker remains active. **SCATTER angular wrap: RED_REPRODUCED → GREEN_VERIFIED / GPU_VERIFIED**, bounded to the cases below.

- Root cause: angular position repeats after a turn, but hash cell identity did not. The same neighborhood across atan2 wrap generated different points. Shared `02_scatter.wgsl` now hashes angular cell X modulo its exact integer period (`param_a + 1`); retains unwrapped coordinates for placement. No added blur, exposure adjustment, new opcode or API. DIRECT3D/TANGENT_LOCAL hash behavior unchanged. Angular seeded layouts change, disclosed in guide.
- Original worker 16-case RED: cylindrical **0.8823242**, polar **0.14477539**. First candidate had no polar points at those sparse sample locations; its nonempty assertion rejected a vacuous pass. Parent expanded to 64 cases per domain while retaining the original cases/tolerance, reverted only the candidate wrap fix, and reran actual pre-fix RED: cylindrical **0.8823242**, polar **0.8647461**. No threshold relaxation or seed tuning in production.
- Same expanded test GREEN: maximum error **0.00048828125** in both domains versus **0.005** tolerance; nonempty RGB aggregates cylindrical **21.583557**, polar **16.473038**. Covers varied elevation/seed/density/size at ±PI offset by 0.00001 radians, DUST profile, decoded axes. This is not every variant/density/pole/cell boundary.
- Commands: `cargo test --locked -p nethercore-zx --test epu_gpu scatter_angular_wrap_continuity -- --nocapture` for RED; `cargo test --locked -p nethercore-zx --test epu_gpu -- --nocapture` for final GREEN: **7 passed, 0 failed**, RTX 3080/Vulkan. Native TMP environment as documented. `cargo build --locked -p nethercore-zx --bin nethercore-zx` and `git diff --check` passed.
- Real baseline: `tmp/epu-review/scatter-wrap-before/captures/20260909-214758-angular-wrap-before-final.png`; candidate: `tmp/epu-review/scatter-wrap-after/captures/20260909-215214-angular-wrap-after.png`. Exact configuration/effective view read back and ROM SHA-256 matched. Receipts with binary hashes: `scatter-wrap-before.json`, `scatter-wrap-after.json` under the same evidence root.
- View: showcase scene 0 (not benchmark), camera angle 90°, elevation -30°, locked, UI/probe off, 960×540. Important source finding: benchmark rendering uses rig elevation instead of the camera elevation control; earlier benchmark readback proved requested values, not effective elevation. Prior comparisons used the same rig and remain matched, but must not claim the control governed those frames.
- Telegram sheet: `tmp/epu-review/scatter-wrap-before-after.png`. Before central point has a sharp vertical cutoff; after it is rounded and continuous in this view. Both frames nonempty. Other point positions change due to periodic hashing; this is a mechanism diagnostic, not aesthetic approval. No new performance acceptance. Capture player stopped.
- Remaining Slice A: low-density duplicate-cell behavior, local-search completeness for large sizes/densities, poles, TANGENT_LOCAL, broader contracts/metadata/neutrality, risk-focused review. Later slices and human acceptance still open.


Separate SOURCE_CONFIRMED authoring discrepancies in `02_scatter.wgsl`, recorded for the ordered metadata/neutrality audit rather than silently redesigning variants:
- Metadata describes `param_c` as whole-byte u8_01 twinkle; runtime reads only its high nibble.
- Metadata advertises fixed size interpolation 0.001..0.05 rad; runtime upper bound is density-dependent `max(0.05, 0.5/density)`, then applies a variant multiplier. Do not change runtime to make a misleading label true.
- WINDOWS/RAIN/EMBERS comments claim rectangular/elongated geometry, but `scatter_point_shape` receives only scalar angular distance and random values—no directional displacement from which to construct a rectangle or vertical streak. These are radial profiles, not demonstrated anisotropic shapes. No new shape feature or variant encoding is authorized by this finding.





## Bounded showcase seam recovery — PARTIAL

FLOW integer-dot fix retained and new fixtures relocated into tests/epu_gpu (no retained tmp include). Post-relocation command: cargo test --locked --offline -p nethercore-zx --test epu_gpu showcase_seam -- --nocapture --test-threads=1.

running 3 tests
FLOW maximum jumps [weight, noise x/y/z] [0.0, 0.0, 0.0, 0.0]; tolerance=0.001
FLOW 771 lattice boundaries +/-128 all axes max_jump=0, range=-0.85791016..0.85058594, tolerance=.001
VEIL historical hard segment gate maximum linear weighted-RGB jump=0.13049316; continuity budget .001 NOT satisfied (characterization only)
test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 27 filtered out; finished in 3.49s

Only the first two are continuity tests; the third characterizes still-broken VEIL hard endpoints, not continuity green. Trail seam correspondence remains UNRESOLVED; no intentional-endpoint dismissal, no trail runtime change, no larger EPU closure. Seeded FLOW pattern compatibility necessarily changes. Retained native capture identities audited separately from host /config isolation; no new native capture claimed. Human visual acceptance outstanding for both. Full bounded result: tmp/epu-review/seam-recovery-result.md; identity audit: tmp/epu-review/showcase-seam/recovery-audit.json.
