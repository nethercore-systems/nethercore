[package]
name = "gen-tracker-common"
version = "0.1.0"
edition = "2021"
description = "Shared utilities for procedural tracker music generators"

[dependencies]
# No external deps - pure Rust

[dev-dependencies]
tempfile = "3"
