# Done Criteria

This pack is done only when all of these are true:

- 20 presets exist in code
- count, names, and animation tables are in sync
- replay scripts match the real roster
- full-sweep capture uses UI-off clean shots
- real replay-driven screenshots have been captured
- each preset has a logged adversarial review
- coverage docs reflect actual opcode, domain, and variant usage
- open gaps are empty or explicitly justified
- no remaining known weak duplicates or technical flaws

