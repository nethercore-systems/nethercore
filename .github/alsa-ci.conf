# Exercise CPAL playback on headless Linux runners without physical audio hardware.
pcm.!default {
    type plug
    slave {
        pcm { type null }
        format FLOAT_LE
        rate 44100
        channels 2
    }
}
